
!build/
!build/quam-manager.spec
# ...but the hand-written PyInstaller spec is source, not an artifact
# Claude Code
# Flask runtime data (per-deployment user state, history snapshots, SQLite indexes)
# IDE
# Local scratch — config-generator verification scripts + their build outputs
# Local-only decoder for the customer-data scrub (real names/paths) — never publish
# Node — only for the headless JS tests (jsdom). Install once with
# OS
# PyInstaller build artifacts
# Python
# Screenshots / reference images
# Session-local scratch: repro/probe/audit dirs + files, build logs (never source)
# Testing
# Virtual environments
# `npm install jsdom`; tests/test_generate_nav.py skips when jsdom is absent.
*.egg
*.egg-info/
*.png
*.py[cod]
.DS_Store
.claude/
.coverage
.cr_audit/
.eggs/
.idea/
.pytest_cache/
.tmp*
.tmp_cr_build/
.tmp_jsdom/
.venv/
.vscode/
CLAUDE.local.md
Thumbs.db
__pycache__/
_cr_audit_probe*.py
_replay_report_local/
build/
build/*
build_stderr.txt
dist/
generated/
htmlcov/
instance.bak.*/
instance/
instance_audit*/
node_modules
node_modules/
package-lock.json
tests/browser/_shots/
tmp_cr_audit/
venv/

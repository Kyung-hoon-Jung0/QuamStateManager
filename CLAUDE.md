# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Note on this file's history**: this file previously accumulated long forensic write-ups of past debugging campaigns (reproduction steps, measured timings, "the first fix was wrong because..." narratives). It was trimmed on 2026-08-21 to keep only current contracts/invariants + doc pointers — the full history for any `docs/NN` reference below lives in `docs/NN_*.md`. Read the doc file before touching related code if you need the "why."

## Project Overview

**QUAM State Manager** is a desktop + web tool for inspecting and editing quantum machine (QUAM) state files. It reads `state.json` + `wiring.json`, resolves custom JSON pointer references, and provides a CLI, Flask web UI, and pywebview desktop app. It also generates fresh QUAM configurations (Generate Config wizard) and previews `machine.generate_config()` output (Config Viewer).

## Commands

**Run tests** (Windows conda env **`cqt`** — the canonical pytest env since 2026-08-16, user-directed. It carries the full customer QM stack — quam 0.6.0 / quam_builder / qualang_tools / qm 1.4.0 plus the customer's own `quam_config` — so it runs the plain suite AND the QM-subprocess tests other envs skip; its stdout carries a `qm - INFO - Starting session` line, filter it when parsing. Conda envs here churn — confirm with `conda env list` rather than trusting any name. `PYTHONUTF8=1` is REQUIRED: the node-selfcheck drivers' subprocess output is UTF-8 and Windows' default cp949 codec otherwise nulls their stdout. `--timeout` keeps any hang from stalling the suite silently):
```bash
PYTHONUTF8=1 conda run -n cqt python -m pytest tests/ -q --timeout=900 --timeout-method=thread
# Single test file:
PYTHONUTF8=1 conda run -n cqt python -m pytest tests/test_pointer_resolver.py -v
```
**Expected `cqt` baseline (docs/127, 2026-08-21): 14 failed, ~5,750+ passed, 247 skipped**, with `--deselect tests/test_main.py::TestWaitForServer`. The 14 are an OS-behavior-difference class documented in `docs/87` (loopback/WSL-kernel checks, tmp-path case-identity tests, a couple of Windows file-locking timing flakes that pass in isolation) — not regressions. `test_autofit_gates`/`test_autofit_synth`/`test_runner_p2` failures (23 of them, `ndarray.ptp()` removed in numpy 2.x) were fixed in docs/127; if they reappear it's a real numpy-2.x incompatibility, not environmental. **Re-derive this baseline in whatever env you actually use** rather than trusting old numbers from other envs (`LabD_17Q`/`qm_mng` no longer exist on this machine).

**The `.cjs` selfchecks need node + jsdom — and they SKIP, loudly-looking-green, without them.** Many pins in `tests/` drive the real shipped JS under jsdom via a Python driver whose gate is `pytest.skip("jsdom not installed")`. Run `npm install` (jsdom + canvas) before trusting a "clean" run. `npm run selfcheck` reports `(N assertions)`, `(silent style)` (legit — `ok()` only speaks on failure), or `ok? … (exit 0 but NO output)` (can't tell asserting-nothing from passing). The harnesses hand Node individual globals (`global.window`, `global.document`, …) rather than a real jsdom script realm — a guard that checks `window.X` while the code *calls* bare `X` throws instead of degrading (the `CSS` global bug, docs/78-era). When adding a harness, bridge every global the code under test reads bare.

**Run web dev server** (use a non-default port — stale dev servers stack up; serve from a Windows env, never WSL — broken loopback forwarding on this machine):
```bash
conda run -n LabD_17Q python -c "from quam_state_manager.web.app import create_app; create_app().run(debug=True, port=5050)"
```

**Run CLI:**
```bash
python -m quam_state_manager.cli show qA1 -f "path/to/quam_state/"
python -m quam_state_manager.cli --version
python -m quam_state_manager.cli --help
```

**Run desktop app:**
```bash
python -m quam_state_manager
```

**Build standalone executable:**
```bash
pyinstaller build/quam-manager.spec
# Output: dist/quam-manager/quam-manager.exe
```
Uses **onedir mode** (not onefile) for instant cold start — onefile extracts to temp on every launch (3–10s overhead).

## Architecture

### Core Pipeline

```
state.json + wiring.json
    → safe_io.read_state_wiring (FILE_SHARE_DELETE + mtime-bracketed pair read)
        → loader.py (QuamStore — owns per-instance pointer cache)
            → pointer_resolver.py (resolves #/, #../, #./ references via store cache)
            → search_index.py (prefix map + trigram index)
            → query.py (flattens nested JSON → qubit/pair dicts)
            → modifier.py (type-coerced edits, undo, rollback)
            → saver.py (atomic writes via safe_io.atomic_write_json, .bak rotation)
            → differ.py (2-way diffs, N-way trends)
            → history.py (Param History: SQLite property index, snapshots)
            → config_view.py (Config Viewer — preview generate_config())
            → config_generator.py (Generate wizard subprocess driver)
```

### QUAM Pointer System

QUAM files avoid value duplication via three pointer types:
- `#/qubits/qA1/f_01` — absolute from root
- `#../x180/length` — relative to parent
- `#./self_ref` — relative to self

`pointer_resolver.py` resolves these **on-read, never in-place**. The cache is **per-`QuamStore`** (each store owns its own dict + lock), so two chips loaded in the same process with same-named qubits never share resolutions. Unresolvable pointers return raw strings + log warnings (real data has dangling pointers). See `docs/32_red_team_phase_2.md` finding 0.1.

### QuamStore (Thread-Safe)

`core/loader.py` holds merged state+wiring dicts, change log, search index, and per-store pointer cache in a single `QuamStore`. All mutations are guarded by `threading.RLock` (Flask workers + pywebview UI thread share state). Loads route through `safe_io.read_state_wiring` so the read never blocks an experiment's atomic save on Windows.

### Working-Copy / Live-File Safety

Experiment programs read and write the same `state.json` + `wiring.json` the app watches; on Windows a plain `open()` for read blocks their atomic `os.replace` save. So the app operates on a **working copy** under `instance/working_state/` (`core/working_copy.py`): the `QuamStore` and `Saver` are built on the copy, background change detection is `os.stat`-only, and the live files are read only on an explicit user **sync** and written only on an explicit **apply to live**. All live-file content I/O goes through `core/safe_io.py` (share-delete reads, `ReplaceFileW` writes, transient retry, mtime-bracketed pair read). LRU eviction in `_activate_quam` keeps the on-disk working folder so a re-load rehydrates unapplied edits via `working_copy.load`. See `docs/28_conflict_safe_io.md`.

**State-roundtrip invariant (docs/65):** content loaded WHOLESALE into the working copy (dataset Load State, State-History stage, Revert last apply) has no change-log entries and a cleared re-apply stash — `/state/sync` must never pull-first over it. A `ctx["staged_base"]` marker overrides the carve-out so staged+edited mixed state still pushes whole. Revert-last-apply's fallback target is content-matched, never the newest snapshot. Pinned by `tests/test_state_roundtrip.py` + `tests/state_sync_selfcheck.cjs`.

**SM never swaps what you are looking at (docs/87).** `reconcile_with_live` no longer auto-pulls on a provably-clean working copy from the user-facing path (`sync_if_clean=False`) — a stale-chip bug's fix used to silently overwrite whatever the user was looking at, which is worse than the staleness it fixed. `auto_adopt` splits by ACTOR, not behavior: the user opening a chip gets `False`; the scheduler's post-node hook and autofit's `reconcile()` get `True` (a robot mid-loop doesn't stop to ask, and autofit's gates need a live anchor). A run started from SM's own Experiment Runner still adopts silently; only writes from OUTSIDE SM raise the drift banner, which now reports "N values differ" from data already read for the verdict (zero added live reads). Pinned by `tests/test_live_replace_routes.py` + `tests/test_sync_robustness.py`.

**A field the entity has not got yet (docs/88).** A cell edit into a leaf whose parent is itself an unresolved pointer used to try to walk into the pointer string. `edit_policy.resolve_missing_leaf_path` only fills in a truly-missing final key (never infers into a list parent); creation stays declared-only via the existing `create` flag, which the grids set from the server's `data-missing` marker. Pinned by `tests/test_missing_leaf_edit.py`.

**The third choice when live drifted (docs/86).** When something outside SM rewrites the live files, the user can now choose a direction instead of only pulling: **↑ Keep mine — overwrite live** sits beside Pull/Sync in both the drift banner and the review modal, gated to skip archives, with a preflight (`GET /state/overwrite-live/preflight`) naming what disappears and one forced confirm (pull and push are NOT symmetric — a pull discards nothing on disk, a push discards live content). A provably-clean working copy still auto-pulls silently (unrelated to this — that's the stale-chip case docs/87 fixed). **One directional-trio vocabulary (docs/97):** every surface now labels the same three actions identically — ↓ Take live / ↑ Keep mine — overwrite live / ⇄ Pull & apply (merge) — instead of six different historical labels. Pinned by `tests/test_overwrite_live.py` + `tests/state_sync_selfcheck.cjs`.

The **load path is content-hash-aware**: each sync point records `synced_live_hash` in the meta sidecar, and `reconcile_with_live` runs on load/select — live replaced + working copy provably clean ⇒ auto-pull; replaced + possibly edited ⇒ keep + `live_diverged` banner (never clobber); unreadable live ⇒ never treated as replaced. Working-copy **GC** deletes only provably-clean copies (transient read failures classify as kept-unverifiable), and its in-use set folds in every LIVE peer window's open chip via `instances.peers()` + `working_copy.key_for`.

### Scanner + LRU Cache + Sidebar Tree v2

`core/scanner.py` discovers experiment folders lazily. `QuamStore` instances are LRU-cached (max 10, ~40MB total) — only loaded when selected in the UI. `node.json` reads also go through `safe_io.read_json`.

**Sidebar tree v2 (r13, docs/68):** `Workspace.tree` stays the flat `{root: [DateGroup]}` data model; `build_nested_tree(root, entries)` derives a VS-Code-style render model per root (real folder levels, date levels descending, leaf entries run-id descending with the 50-cap keeping the newest 50). Staleness = a structure-spine probe: per root, the recorded set of run-parent dirs + ancestors is re-statted as a `{dir → mtime}` map. `rescan_root` scans outside the lock and swaps the tree in one rebind. The active run's ancestor `<details>` get `.tree-branch-active` so a collapsed tree still shows where the open experiment lives.

### Web UI (HTMX)

Flask routes return either full pages or partial `_*.html` template fragments for `hx-swap`. Partials are named `_*.html`; full pages are named without underscore. No React/Vue — HTMX + ~5,100 lines of vanilla JS + ~3,000 lines for the Generate-Config wizard. Routes in `web/routes.py` (~3,800 lines, ~90 endpoints). Templates: 63 files (17 full pages + 46 partials). All colors are parameterized as ~430 CSS custom properties with light/dark theme support.

Clicking a Plotly data point in a dataset figure opens a confirmation popup (one row per affected dot-path, editable, per-row Apply + Apply All) rather than auto-applying. See `docs/36_plot_click_confirm_popup.md`.

**Δ everywhere (docs/76).** Every before→after surface renders old/new/difference through ONE implementation: `core/value_delta.py` (+ the `value_delta` Jinja filter, `_delta_macros.html`) server-side, `window.ValueDelta` client-side — pinned character-for-character (`tests/test_value_delta.py`). Subtraction is exact decimal; a Δ renders only where it means something (booleans/nulls/pointers/strings/created-deleted subtrees render nothing, never a fabricated 0). Wired into essentially every diff/compare surface in the app (Review tray, sync screen, plot-click popup, Prev-State diff, Compare-selected, value/Column History, autofit ledger, `/changes`, undo toast).

The qubit/pair inspector panes carry a sticky in-panel search bar (`filterDetailPanel` in app.js — AND-tokens across all `<details>` sections, auto-opens matches, "X of Y" count).

**The diff workbench is the front door (docs/84).** `/diff?a=<ref>&b=<ref>&tab=state|wiring|node|data&view=tree|list` is the one comparison surface (tree = the Explorer's `renderJsonTree` with a diff overlay; "Differences only" is a pruned document via `core/json_diff.py`, ranked by |Δ%|, pruned to changed leaves + ancestors). Every legacy comparison entry point (`/compare`, `/chip-compare`, sidebar Compare) redirects into it; the Compare hub sits behind "Advanced…" for entity mapping + N-way baskets. Pinned by `tests/test_diff_workbench.py` + `tests/diff_tree_selfcheck.cjs`.

**One search grammar (docs/96).** space = AND, standalone `|` = OR (tight binding), every other pipe literal. `web/static/search-query.js` + `core/search_query.py` are JS↔PY parity-pinned; wired into tree search, bulk/pair grids, Datasets table, sidebar filter. **A token that NAMES a row wins the row axis (docs/110)** — both grids classify each token as column/id/value with exclusive precedence, so a qubit-id token that also happens to appear in a column's search text no longer gets swallowed as neutral. Pinned by `bulk_search_selfcheck.cjs`.

Comparison also lives in the **Compare hub** (`/compare-hub`) — one URL-canonical surface (`src=` ref tokens + `bucket`/`preset`/`ref`/`map`) with three declared contexts (same chip over time / same design different device / different devices). Engine = `core/compare.py` + `core/compare_sources.py` (isolated source pool). Snapshot-vs-snapshot for a single chip stays in Param History. See `docs/49_compare_hub_redesign.md`, `docs/35_chip_compare.md`.

**What actually leaves the instrument (docs/109).** Every amplitude on the entity surfaces shows its true physical output: MW channels show dBm via the FSP identity `P = FSP + 20·log10|amp|`, flux/LF channels show volts. `core/physical_units.py` (pure): `channel_of` walks ≤4 ancestors to the port dict. Honest by construction — text values, MW amp 0, dangling chains, no channel ancestor ⇒ blank, never invented. Surfaced as a muted sub-line in bulk grid cells + the inspector's `phys_amp` filter. Pinned by `tests/test_physical_units.py`.

**A tab keeps its state; the grid works like a spreadsheet; failures explain themselves (docs/110–115).** A seven-workstream daily-flow campaign. **PaneState (docs/110)**: main-pane keep-alive across htmx nav — parks the outgoing pane's DOM at `beforeSwap`, restores at `afterSwap` when the tray's `mutation_seq` + chip token both match, else the fresh render stands (never cancels the htmx request itself). **docs/111** adds same-column multi-select, Ctrl+D fill-down, multi-line paste as one undo action, pinned columns, dyn-column reload that carries unsaved edits. **docs/112** adds j/k/Enter/Space/Escape run navigation + a digest band over the filtered set. **docs/113** ships `/` focus-search, Ctrl+Enter on armed Apply-all, a `?` cheat sheet. **docs/114** makes failure honest: a non-chip folder offers every real subfolder, a dangling pointer says DANGLING, write permission is probed at open. **docs/115** gives the manual a permanent `/help` address and a real onboarding CTA. Pinned by `tests/test_pane_state.py`, `test_grid_editing.py`, `test_ds_flow.py`, `test_kb_polish.py`, `test_honest_failures.py`, `test_onboarding.py` + matching `*_selfcheck.cjs`.

**Live State Edit shows everything (docs/85).** Every curated and derived column defaults visible now (hiding was a rendering decision the search box didn't know about — `T1` was hidden while ~200 obscure derived leaves showed). `headline_on` is a separate flag from `default_on` for which pair ROWS Compare summarizes (kept byte-identical to the old formula under a new name). Search always scans every column regardless of visibility, with a hint chip to reveal hidden matches (never auto-reveals; hidden values never enter the search haystack). Persisted column choices are versioned (`…_cols_v2`) so the visibility flip reaches everyone. Pinned by `tests/test_bulk_edit.py` + `tests/test_pair_columns.py` + `tests/bulk_dyncols_selfcheck.cjs`.

**Settings + Calculator are sidebar tools (docs/89).** Moved from the far top-right to the first row of the sidebar (icon + label above a divider). Collapse traps handled: a compact icon-only pair shows only while the sidebar is collapsed (not a duplicate — `.sidebar-collapsed` is `width:0`, not an icon rail), and both popovers are body-level positioned in viewport coords (the sidebar's `overflow-y:auto` would clip an absolutely-positioned child). Icons are inline SVG so they inherit theme + UI scale. **Calculator (docs/100)**: formulas audited exact; a fourth RF=LO+IF section added, solver pure and DOM-free-pinned. Pinned by `tests/test_sidebar_tools.py` + `tests/sidebar_tools_selfcheck.cjs`.

**Chip topology as a picture (docs/92, plan docs/91).** `TopoGraph.layoutFor` centralizes three honest states: `physical` (only when every node passes a strict grid gate), `logical` (derived from pair connectivity alone, with a visible note), `none` (one honest line, never an invented map). Two maps, one division of labour: the **Chip Status hero map** carries numbers (per-node metric values, edge metrics, hover popup); the **component-page map** (`TopoGraph.renderLayout`/`ComponentMap`) carries no numbers — only structure (stones, pair edges, resonator marks, flux stubs, feedline buses), with map↔table hover binding both ways. Pinned by `tests/topo_graph_selfcheck.cjs` + `chip_status_hero_selfcheck.cjs` + `component_map_selfcheck.cjs`; verified on 8 real chips.

**The schema-drift healing chain + the silent grid cap (docs/94).** A lab's out-of-band class migration exposed three breaks in the healing chain (manifest cache key now folds in the class set; the docs/79 warm-carry now gates on class-inventory coverage, not just version; the three content-entry choke points now re-attach/re-probe). The Live-Edit qubit grid's dynamic-column cap was silently truncating real 452-column chips (`MAX_DYNAMIC_COLUMNS` 400→1200, and a tripped cap now renders a visible note). Pinned by `tests/test_schema_heal.py` + `tests/test_bulk_edit.py::TestDynColumnCapHonesty`.

**Every pulse class, generated and loadable (docs/98).** An all-pulse-class verification round caught two `Quam.load()` killers reachable from the UI: class-prefix acceptance now checks against the SELECTED env's own importable homes (not just catalog membership), and `pulse_catalog.env_field_filter` drops template fields the env doesn't know about before writing. A gate-slot pulse's leaves are now indexed per-leaf like `create_subtree`. Pinned by `tests/test_pulse_env_canonical_home.py`.

**Component-map feedback r1 (docs/93).** Five user items, each root-caused: chain-tab computation order fixed (a line-move, `tests/test_chain_tabs.py`); map grew 1.875× with id font derived from the cell as an SVG attribute (a stylesheet rule beats a presentation attribute — this project has hit that trap three times); active chain filter lights its stones; feedlines get identity colours on the resonators page (7 hues, dataviz-validated both themes, spatial slot order, per-slot dash as secondary encoding); frequency-inequality chevrons on pair edges (one per physical pair, gated on both-numeric + |Δ|≥1MHz). Pinned by `component_map_selfcheck.cjs` + `test_chain_tabs.py`.

**Sidebar IA (r15, docs/69):** structure→health order — Instrument Wiring · Chip Components subnav (Qubits/Pairs/Resonators/Flux/Couplers) · Diagnostics · Chip Status · Compare · Live State Edit ▾(Json Tree View, Pulses) · State History ▾(Param History) · Experiment Runner / Fit Replay / Auto Calibrate (routes `/scheduler` `/fit-audit` `/autofit`, page tokens unchanged) · Datasets ▾(Collections, Trends). Pins in `TestSidebarIAr15`/`TestActiveMarkingR15`.

UI state persisted in `localStorage`: `quam_font_size`, `quam_sidebar_collapsed`, `quam_tray_open`, `quam_split_sizes`, `quam_theme`, `quam_exp_list_compact`, `quam_qualibrate_nav_collapsed`, `recentFolders`, and the Live-Edit column sets `quam_bulk_hidden_cols_v2` / `quam_bulk_hidden_cols_pair_v2` / `quam_bulk_dynhidden` (all HIDDEN sets — absent means show everything).

Modal focus traps (`trapFocus` in app.js) are leak-proof at the source: re-trapping a container releases its previous trap, and a trap whose container went hidden/detached self-heals on the next keydown. Both Live-Edit grids hop Tab/Shift+Tab between edit cells; the topbar calculator hops its visible inputs. Pinned by `tests/tab_focus_selfcheck.cjs` + `tests/test_tab_focus.py`. See `docs/64_tab_focus_calc.md`.

**Customer feedback rounds 2–3 (docs/126, 2026-08-19) — real-browser verified against the PJ_10082026 20Q QDAC chip.** Highlights: `pair_columns` now mirrors `qubit_columns`'s port-chain expansion (on a QDAC chip the coupler, not the qubit, is wired to the OPX flux port — those fields existed on no grid before); topology hero metric patches, 2Q edge metrics, pane-width zoom; Live-Edit patch-render time cut ~2× (batched column hydration, one generated stylesheet instead of ~9,000 per-cell class toggles); Json Tree gained `ExplorerChips` port-owner chips + row-copy; **Apply-to-chip (round 3, user-directed) now applies over pending edits/live drift WITHOUT asking**, naming what happened in the result line — this is the current behavior, superseding the older docs/108/116 gate descriptions below for this one button; a floating Instrument Wiring panel; `CosineBipolarPulse` added to the pulse catalog; `core/gaussian_cz.py` transcribes the customer's own CZ-macro planner as one Pulses-page button. Auto-Sync pill/panel restyled, chip Versions panel relabeled with a quick-diff of the two newest snapshots, topology role-marker/metric z-order fixed, outlier scoring made spec-verdict-first (a `pass` value is never flagged), sidebar search debounce/sync fixed, and a printable Chip Status report (`GET /chip-status/report`) added. **#22 digital triggers (2026-08-20):** the Instrument Wiring diagram now scans every entity for `digital_outputs` (pointer chains followed, deduplicated per physical line) and renders a DIG sub-column per FEM; chips with no digital wiring render byte-identically. Pinned by `test_pair_columns`, `test_gaussian_cz`, `test_explorer_features`, `test_misc_ui`, `test_dataset_apply_to_chip`, `test_state_versions`, `test_report_card`, `test_instrument_digital`, plus matching `*_selfcheck.cjs` files.

**Versions panel: per-row Diff + differences-only N-way Compare (docs/128, 2026-08-21).** Every version row carries a read-only **Diff** button — this-version-vs-NOW in the sync review modal's shell + row language (engine: the pre-existing `HistoryManager.diff_current`; NONE of the sync flow's write actions ride along — Pull to Live stays the one gated write, and the note naming it renders only where that button actually exists). Tick-3+ Compare now lands on `/diff/versions`: one row per differing dot-path, one column per version, agreeing leaves never rendered (`Differ.diff_n` — every leaf, per-cell `changed` computed with the row verdict's own equality, docs/118); the Compare hub survives as that page's "Advanced" link. Both new routes pass `ignore_keys=set()` so a class migration (docs/94) is a visible difference rather than a silent one — the page TELLS the user every other leaf agrees, so it must not skip `__class__`; `diff_n` also treats two NaNs as agreeing (docs/118's rule, not a third answer). **Compact means adjacent, not narrow**: the row's actions render between the date and the meta (`margin-left:auto` removed) — the first attempt narrowed the panel 46→36rem instead and that measured as a regression, crushing the docs/126 quick-diff table it shares width with. Two adversarial review rounds (the second a 41-agent red team against the shipped commit: 34 raised, 4 confirmed + 1 from the completeness critic) found 14 defects, all fixed — notably `smModalOpen` not knowing the new overlay (global shortcuts fired behind the modal), a chip-identity gate for two-window races, a stale-response token, an N-way key column collapsing to one character per line, and sticky headers that never stuck. Pinned by `TestVersionDiff`/`TestVersionsCompareNway`/`TestCompactRows` + `tests/version_diff_selfcheck.cjs` (the JS pins were grep-only until the review; the selfcheck executes them and is mutation-checked).

**Versions answer WHO/WHY, not just WHEN (docs/132, 2026-08-22).** Customer round 2. Every row wears ONE of three kinds in the user's vocabulary — **EXP** (a run produced this state) / **MANUAL** (deliberate user change: apply, pull, restore, snapshot, bookmark) / **BACKUP** (protective pre-write copy) — via the additive `SnapshotMeta.kind` stamped at all 14 capture sites (raw `trigger` untouched; legacy snapshots display through the single `kind_for` mapping, flagged "likely reading", never claimed certain; `restore` finally has a label anywhere). EXP rows carry a clickable **After #\<run_id\>** chip (qualibrate saves state post-fit — "after" is the honest word) opening the run's data panel; **only where a run is recorded, never a time guess**. Recording is made real by near-real-time ingest: enqueue-only detection (delta poll aggregation / scheduler hook / a live-write mtime EDGE on the drift poll — two stats; the budgeted scan runs on the worker), a gated worker calling `HistoryManager.ingest_run` (fingerprint gate verbatim from `_runs_field_series`, chip decisions respected, mid-write retries, cross-process idempotent, REAL diff_summary), the scheduler hook REORDERED so ingest precedes the adopt (hash-dedup collapses the duplicate — attribution is never threaded through the docs/87 choke point), and **both orders converge**: identical content dedups forward, enriches (annotates run fields onto the existing row) backward. `hist_seq` rides `/state/drift` so open panels follow other-process captures (healing the old two-window staleness). Panel defaults to **changes-only** rows (zero-diff copies hidden with an honest count; pinned/EXP/current/labeled exempt) and states retention truth ("All N kept"); the current-version hash scan is O(1) now. **Per-value ✓ take** on all three compare surfaces (overlay / N-way / workbench gated to one-`working:`-side + state/wiring tabs) stages single values into the working copy via `/field/edit-batch` with `expect_chip` — live untouched until Apply. Dataset poll 15s→5s (pre-existing budget/backoff guards); new-run popup closes via ✕/Esc/backdrop. Pinned across `TestKindStamping`/`TestRunIngest`/`TestHistorySeq` (test_history), `TestKindChips`/`TestExpRow`/`TestChangesOnlyFilter`/`TestPanelFollowsIngest`/`TestPerValueTake`/`TestWorkbenchTake` (test_state_versions), `TestExpIngestOnPoll`/`TestSchedulerHookOrder` (test_poll_stability), extended `version_diff_selfcheck.cjs` (38 asserts; caught a real bare-`showToast` guard/call mismatch — the standing harness rule — the day it was written). A same-day 35-agent heavy review then confirmed 27 findings + 4 from the critic (one rejection in 28), all fixed — three CRITICAL: a double-drain race whose dedup branch deleted the just-created snapshot and poisoned re-ingest (drains now claim-and-serialize; ingest_run runs under the manager lock), double-quoted `tojson` attributes staging "" for every string take (single-quoted now, the documented Flask contract), and run-local wall-clock stamped into the UTC snapshot namespace (both stamp derivations convert LOCAL→UTC together). Full ledger in docs/132's review section.

### Multi-Context Registry

`app.config["contexts"]` maps names → context dicts with a `type` field (currently `"quam"`). Designed to accept HDF5/dataset types without restructuring. Routes use helpers (`_store()`, `_engine()`, `_modifier()`, etc.) to pull from the active context transparently.

### Param History

`core/history.py` (`HistoryManager`, ~2,300 lines) keeps timestamped snapshots of each chip under `instance/history/<chip>/` plus a per-chip SQLite property index (`index.sqlite`) for fast trend queries. Content-hash dedup, fingerprint-aware multi-chip routing, alignment scan against the workspace, chip-decisions persistence, v1+v2 migrations. Triggers: `save` / `manual` / `auto` / `experiment`. See `docs/20_param_history.md`, `docs/21_multi_chip_support.md`, `docs/23_param_history_performance.md`.

**Chip identity is a ladder** (docs/20 v2): `state.json`'s top-level `extras["chip_name"]` (user-declared) **>** hardware fingerprint **>** legacy path-derived name, resolved through the single `HistoryManager.resolve_chip_dir` choke point. chip_key = canonical on-disk dir name; pretty names live in `instance/history/_chip_aliases.json` (token-mismatch heals via alignment against the claimed dir's newest sample — only different-network AND different-labels refuses). First open of an unnamed live chip shows a "No chip name is set" banner staging `extras.chip_name` (+ optional `extras.data_folder`) through the working copy only.

**Every numeric parameter, as change points (docs/83).** `core/leaf_index.py` indexes only the TRANSITIONS between consecutive snapshots (median 2–4 changed leaves out of ~8,000 on a real chip), inside the chip's existing `index.sqlite`, and ends up smaller than the curated-eleven index beside it. Pointers are followed. Ordering is rebuilt (not incrementally repaired) on out-of-order arrival; a rebuild MERGES rather than replaces, so pruned-snapshot history survives. Surface: `/param-history/changes` (Trends | Changes tab strip), paged by snapshot. Pinned by `tests/test_leaf_index.py` + `tests/test_param_history_changes.py`.

**Column History + LiveEditUndo** (docs/20 v2b + r9): every bulk-grid column header carries a hover 🕘 opening a two-tab panel (Changes = per-row change-point chips; By run = last-6-matching-runs with Use all). **Ctrl+Z is tiered**: wizard undo → `LiveEditUndo` (in-memory, un-staged edits) → server `POST /undo` (`modifier.undo_group`, one Review-tray group per press). Both apply paths snapshot the pre-apply live state first, arming an explicit "Revert last apply" tray button.

**Dataset "Apply to chip" (docs/108, amended by docs/126 ⑤ — read that entry above; the identity/pending gates described here for OTHER apply paths still apply elsewhere).** The State tab's primary action stages the run's snapshot then pushes through the shared apply core in one press; the pre-apply snapshot arms Revert last apply. `apply_to_live` also has an identical-content fast path (docs/116) — a write that is provably a no-op advances the sync point instead of raising a spurious staleness conflict. Pinned by `tests/test_dataset_apply_to_chip.py`.

**Ctrl+Z crosses the save boundary; Ctrl+Shift+Z is redo (docs/107).** The three save sites journal the outgoing change log as user-action units (`core/undo_journal.py`, sidecar `instance/working_state/<key>.undo_journal.json`, bound 200, two-phase — appended only after the save succeeds). **Covenant: any direct live write requires ≥1 explicit Apply-to-live press** — a journal step only stages its inverse into the tray (gid `jrn:<unit-id>`), and the armed session (docs/117) flushes it through the same door. `/undo` routes by the top gid alone; all-or-nothing with LIFO rollback + 409. Pinned by `tests/test_undo_journal.py` + `ctrlz_selfcheck.cjs`.

Every editable value carries a 🕘 value-history popover — `GET /field/history?path=` → `HistoryManager.field_history`: change-point timeline (tracked props via SQLite; other leaves via capped scan; pointers resolved per-snapshot) merged with a runs tier, with **Use** (fills the edit input) and **Data** (loads the run's detail).

The auto-incremental backfill on page open is single-attempt per chip per browser-tab session (sessionStorage marker). Failed entries are captured and surfaced via an amber banner on `/param-history` with a "Retry import" button.

### State History

`/state-history` (sidebar; Param History nests under it since r15/docs/69) is a view + restore layer over the same `HistoryManager` snapshot store (no new capture path). **Mode 1 stage** (`/state-history/<ts>/stage`) loads a snapshot into the working copy for review→Apply; **Mode 2 restore-live** (`/state-history/<ts>/restore-live`) replaces the live chip directly. Both write through the single build lock and rebuild every derived cache via `_rebuild_after_working_copy_replaced(ctx)`. restore-live's safety gates are independent: origin≠live (archive→409), unsaved edits, wiring-topology mismatch — one token never collapses two gates, and the current live is snapshotted first so a restore is reversible. SnapshotMeta carries `label`/`pinned` (prune-exempt). See `docs/41_state_history.md`.

### Typed Editing + State↔Env Validation

Every leaf of state.json (and wiring.json) is dynamically editable, type-safely. **Path grammar**: list/matrix elements use dot-form numeric segments (`confusion_matrix.0.1`), disambiguated structurally from number-keyed dicts by the actual parent container; strict `^\d+$` index gate.

**Type system** (`core/type_policy.py` + `core/state_env_validate.py`): a key's expected type resolves layered — env schema > per-key user assignment (409-confirmed `override_env`) > value inference — through one walk-down resolver and one judge. Invariants: pointers + null always writable; int widens into float; integral floats pass on int-fields verbatim (never rewritten). With no schema/assignment, behavior is byte-identical legacy `_type_coerce`. Assignments persist in `instance/type_assignments/<chip-key>.json`.

**Env schema probe** (`generator/probe_state_schema.py` + `core/state_env_schema.py`): dumps the selected env's dataclass field schemas for the chip's class inventory (version+commit-keyed cache).

**Type visibility (r14, docs/56):** floats display as "real". Stored-as-text numerics are surfaced actively via `diagnostics.numeric_string_leaves` and a delta-gated alarm banner. **One-click repair (docs/77)**: the alarm's "Fix N values…" → `GET /type-fix/plan` (proposal, refuses identity/membership keys, env-schema-`str` fields, leading zeros, separators) → `POST /type-fix/apply` (one Ctrl+Z group; live untouched). **The alert raises itself (docs/78)**: `_arm_type_alarm(ctx, reason)` fires at the three content-entry choke points and `GET /type-alert` consumes the flag one-shot. The Diagnostics "Types & values" card is the single repair entry point. Pinned by `tests/test_type_autofix.py` + `tests/type_alert_popup_selfcheck.cjs`.

**Env baselines + user verdicts (docs/79):** `core/state_env_baseline.py` records each probe under `instance/state_schema_baselines/<env_key>.json` keyed by env identity, giving class/field diffs across generations. `core/type_verdicts.py` is the user's correction store (`accept` = stop asking / `override` = a TypeSpec stored verbatim), applied as one manifest overlay at the single `load_policy` choke point. Layering: `user(override_env) → verdict → env → user → inference`. Dormant with no verdict file; an accept-all overlay is a provable no-op (golden-pinned).

**Band-edge headroom is SM's own guideline, not QM's (docs/81):** `BAND_EDGE_MARGIN_HZ` is 5 MHz (only an LO practically on the boundary warns). `extras` is treated as free-form text everywhere (no numeric-string alarm, no type-fix offer, verbatim storage) via `edit_policy.is_free_form_path`.

**Validation** rides Diagnostics (domain "Environment match"): aggregated `unknown_field`/`unimportable_class`/`missing_required` errors + type/version warnings. Merge-blockers: a corpus harness (zero blocked re-commits + byte-idempotence over 30 real chips) in `tests/test_type_corpus_idempotence.py`; golden manifests in `tests/golden/state_schema_{modern,fork}.json`. See `docs/56_dynamic_typed_edit.md`.

### QUAlibrate Projects

**Projects** (sidebar, with a toggleable per-project submenu) surfaces the user's `~/.qualibrate` folder — root `config.toml` plus `projects/<name>/config.toml` partial overlays. **Phase 1 is 100% READ-ONLY** (the No-Conflict Doctrine, docs/55) — qualibrate's own root write is non-atomic/unlocked/comment-destroying, so SM must never write these files, pinned by a byte+mtime-identity route test. `core/qualibrate_config.py` owns the reader: `QUALIBRATE_CONFIG_FILE` + `QUAM_STATE_PATH` env semantics, a UI-chosen location override (`instance/qualibrate_location.json`), Windows↔WSL path mapping, 0.1.12 merge fidelity, a `lint` doctor, and `tray_status` (stat-keyed cache). `/qualibrate` is the read-only Config Manager page; `POST /qualibrate/open` is "Open in SM". Topbar `⚗ <project>` badge: red = active project's state_path dangling, amber = SM has a different chip open. See `docs/55_qualibrate_projects.md`.

**Project-centric shell (docs/63).** The project is a lens over the same underlying stores, never a data wall. Sidebar: Projects first, then State Load, then Generate Config; `GET /` is a project-first landing that never auto-activates the last chip. The scope `ctx["qualibrate_project"]` is derived on every activation via a stat-cached reverse index, and pinned by explicit `/qualibrate/open`. History snapshots are stamped with a display-only `SnapshotMeta.project`; Datasets/Trends seed folder selection from `instance/project_dataset_roots.json`. See `tests/test_project_scope.py`.

### Autofit (one-button automatic fitting scheduler)

**Autofit** (`/autofit`; sidebar label "Auto Calibrate" since r15/docs/69) is the closed calibration loop: one button runs a plan of nodes over chosen targets, gates every fit, LLM-audits residual suspects, reverts rejected node writes deterministically, re-measures with adapted parameters, and reports from an append-only ledger. **Doctrine (docs/47, binding): the LLM never emits a number** — its schema is `{verdict, failure_mode, reason}`; corrections are ①deterministic gates ②CAS'd revert via `patches[].old` ③re-run with family-keyed adaptations; calibration numbers always come from the node's own fitter. `core/autofit/`: `families.py` (metric bands + plausibility/jump limits vs the pre-update anchor + feature x-checks, code-curated), `gates.py` (G1 outcome → G4 physical bands → G3 raw-data x-check → G2 metrics/consistency → G5 history), `auditor.py` (per-plan call budget, numeric emissions discarded+flagged), `engine.py` (per-target decisions, hard-criticality halts only that target's chain), `writer.py` (the ONLY write path: build lock → `batch_set` → `saver.save` → `apply_to_live`), `synth.py`/`simbackend.py` (sim chip + corruption modes, indistinguishable to any SM reader), `realbackend.py` (drives the Scheduler chassis). While a plan runs, the scheduler edit-lock and every `/scheduler/*` route 409. See `docs/56_autofit_scheduler.md` (incl. §7b design-review amendments).

**Runner + AI calibration agent (docs/78 — read §17 before trusting any earlier section).** Turns Autofit into a one-button bring-up loop over the 9 families of the x180 chain: press → experiments run → fits gated → state updated → done when gates PASS and a vision judge accepts the signature. **Shipped: P0–P8 + §17 B3. Open: P3c/P3d (needs an API key), P9 (needs hardware).** Doctrine amendment (D-1): a number the AI emits never reaches state.json before passing a tier-A/tier-B verifier. A verification context is the triple (env × analysis-tree revision × run generation) — `envmatrix.py` classifies it, `sourceroot.py` materializes a pinned git revision read-only. `families.py` bands are corpus-derived, never invented (replay real runs, split by the node's own verdict). Update targets are run-derived (flux-point routing, `{operation}` filled and followed through the alias pointer). The vision judge's family knowledge is versioned data (`judge_pack.py`) with shape/geometry-only exemplars enforced at load. `auditor.py` carries four number-free asks: `judge`/`signature`/`compare`/`triage`, all with safe unavailable-defaults. A target is done only when gates pass AND the judge signs off, or — with no judge configured — on gates alone, stamped `vision=unavailable` so no report implies an unperformed check. Also shipped: `action_space.py` (bounded proposals, `load_data_id` blocked on the agent path), `stoploss.py` (three tiers), `consistency.py` (cross-run review), `notify.py`, `replay_score.py` (offline scoring by fewer-runs-to-conclusion). Every verdict carries the context it's valid inside (`verification.py`) — values from different contexts are never compared. `power_rabi`'s `verify_wide` is a mode switch (two distinct experiment shapes), not a scaled span.

**Auto-calibration verified against the CQT corpus (docs/127, 2026-08-21 — 0.9.9 groundwork).** Every offline verification layer of docs/78 executed against the customer's real 2,655-run archive; seven defects found and fixed. Notable: `run_autofit_replay.py` now unwraps `run_params` correctly (it was falling back to defaults for every knob); G3's reader gained `_VAR_EQUIVALENTS` for state-discrimination and coupler-cube renames (verified renames of the same physical trace, identity still guarded by target membership); ramsey recalibrated per the §15.2 corpus method (the decay band deleted — zero measured detection — with write-honesty moved into a physical-window + error-bar guard); the feature check became three-zone (per-family z_min floor + a sign-agnostic claim-region test in the weak zone, since this chip's feature is a dip as often as a peak); `synth.py`'s `arr.ptp()` → `np.ptp(arr)` (numpy 2.x removed the method — a real bug, not environmental, and the cqt autofit test baseline is now 0 failures from this). Remaining open: rfo/res_spec/coupler-flux accepted-claim fails un-adjudicated by the same method; the closed loop needs hardware; the vision judge needs an API key. Pinned by `TestTraceVarAndPairCoordEquivalents`, `TestThreeZoneFeatureCheck`, `TestSandboxRevertWalksLists`, `TestRamseyCqtRecalibration`, `test_the_replay_runner_uses_it_too`.

**The red team, and what it cost the campaign that invited it (docs/123–125).** An adversarial multi-agent review of the docs/122 fix campaign's own claims returned 2 critical + 19 major findings, all independently re-executed and confirmed, and a 12-fix follow-up (docs/125) closed all of them. Load-bearing fixes: htmx 2.0.4's attribute SETTLE was restoring id-matched elements' class/style to markup values ~20ms after every swap, wiping script-set state (disarmed app-wide via one `attributesToSettle` meta); a diff-mode toggle desync was fixed by deriving diff-mode truth from the DOM itself; a Plotly resize approach that permanently froze charts once `layout.width`+`height` were both set was replaced with snapshot-restore (apply width, then hand `gd.layout` back byte-identical). Also: dead live-diff accept/reject buttons (cross-IIFE bare calls), the undo repaint keying on alias paths instead of resolved ones, undo presses swallowed during an in-flight tray apply, and a transposed fringes recipe — all fixed and pinned across `settle_config`/`livediff_buttons`/`fh_chart`/`scroll_abort`/`plothost`/`undo_repaint` selfchecks + `test_revert_payload.py` + extended `ctrlz_selfcheck.cjs`/`test_ndview_axis_order.py`/`test_ramsey_vs_coupler_flux.py`. Standing harness rule: Node-realm eval does not expose window properties as bare globals — bridge them or the miss is swallowed silently.

**Four reports, and the fix was never the first guess (docs/122, extended in Phase 2).** Same reproduce-on-the-real-chip method as docs/120. ① The Raw-data tab's axis default was choosing x by array size instead of by name, opposite to Interactive's convention; `_AXIS_RANK` is now derived from 27 corpus constraints, topologically sorted, gated on full-cube coverage (a partially-covered file is unchanged). ② The Json Tree View search stopped working in diff mode because the diff re-render never re-ran the search filter — fixed, and the SOFT-tier restore now also captures dot-path expansion state (never a DOM index). ③ Ctrl+Z: htmx was silently discarding queued presses from a shared tray source; `window.UndoQueue` now queues instead, and one press no longer triggers a full grid re-GET (2,418ms → 55ms) — the response already names every reverted path, so the grids repaint just those cells. ④ Trends: nothing resized a Plotly figure when its container changed (Plotly's `responsive:true` only listens to window resize); `window.PlotHost` now centralizes container-resize observation + destroy/purge for all 15 Plotly mount points app-wide, chosen only after three design-probe rounds refuted the naive "observe inside the render function" approach (which can't identify the right container, doesn't scale to 220 charts on `/trends`, and two mounts bypass the render function entirely). Pinned by `tests/test_ndview_axis_order.py`, `tests/test_explorer_search.py`, extended `ctrlz_selfcheck.cjs`/`ndview_selfcheck.cjs`/`interactive_stability_selfcheck.cjs`.

**A press means what the presser could see; a lie in a variable is a lie everywhere (docs/120).** Four mechanisms found driving a real 20-qubit chip in real Chrome. ① **The unseen-edit gate**: two SM windows share one server context and change log, so a tray in window B could be stale while its Apply button still fired on window A's edits — both live-write doors now compare the presser's declared `seen_changes` against the log and 409 `unseen_changes`, naming the paths, with `force=1` and `ack_unseen=1` kept as two separate tokens. ② The app's CSP forbids `unsafe-eval`, so all `hx-on::after-request` handlers were silently dead — replaced with `data-after-request` + one delegated dispatcher. ③ `--topbar-height` was a stale CSS literal versus the real measured height; `window.TopbarHeight` now measures and publishes the truth on load/resize/swap. ④ `live_diverged` now self-refreshes via `_refresh_live_diverged` instead of only on a full render. Grid virtualization gate is now cold-cell-count, not total-cell-count, and per-cell geometry reads are coalesced into one rAF. Pinned across `tests/test_unseen_edits.py` and the extended undo/grid selfchecks.

**Auto-apply — the covenant amended (docs/117).** A direct live write happens on an explicit Apply press **OR** inside a user-enabled auto-apply session (default OFF, always visible, auto-disarmed on conflict) — the floor (one explicit act) didn't move, its scope did (the act now authorizes a session). `/state/apply-to-live` stays the ONE live writer; the session is a `MutationObserver` on the tray (`web/static/auto-apply.js`) that flushes immediately and coalesces anything arriving mid-flight into one more flush, sharing `window._applyInFlight` with manual Apply. The applied log is the undo journal labelled (`meta.src == "auto"`), whose ✕ is compare-and-swap so a later write to the same path can't be silently clobbered. Cannot arm on archive/read-only/already-diverged chips. Pinned by `tests/test_auto_apply.py` + `tests/auto_apply_selfcheck.cjs`.

### FSP → Amplitude Compensation

Editing a port's `full_scale_power_dbm` keeps pulse powers constant only if the amplitudes rescale (`P = FSP + 20·log10|amp|`). **Hard contract: amps are never updated quietly and the FSP edit never commits before the user saw the offer** — `/field/edit`(+`-batch`) 409s with `mw_fem.fsp_compensation_plan` unless `fsp_ack=comp|solo`; an accepted `comp` bundles FSP+amps into one gid (one Review bundle, one Ctrl+Z). Wired everywhere an FSP field is editable; un-wired callers get an honest 409 error string. The conservative identity-confirm banner (chip-name mismatch) shares the same never-automatic doctrine. Pinned by `tests/test_fsp_compensation.py` + `TestIdentityConfirm`.

### Dataset Support

`core/dataset.py` (DatasetStore) + `core/experiment_data.py` load experiment runs from `node.json` + `data.json` + HDF5 files, routed through `safe_io` so a still-active experiment's writeback isn't blocked. A separate `_h5_lock` guards h5py reads. Tag/bookmark/note mutations roll back on disk-write failure. `_data_json_cache` is an LRU bounded at 200.

**Multiple windows on one machine (docs/80).** `core/instances.py` registers every live SM process (`instance/instances/<pid>.json`, one file per process, liveness by PID probe) — bookkeeping only, never blocks. **Run ownership**: `run.owner_pid`/`owner_port` are claimed at `start` and released at every terminal transition; a live foreign owner makes `start` raise `ForeignRunnerError` → 409 (the one hard refusal — two workers on one OPX is never safe); no/dead owner is byte-identical to pre-fix behavior (crash recovery). Only the SAME state path open in two live windows raises a warning banner. **Two chips at once**: runner state is keyed by chip under `instance/scheduler/<chip>-<fs_key hash>/`; machine-wide settings (env, calibrations folder, timeout) stay shared at `scheduler/_shared.json`; the UI heartbeat is deliberately NOT chip-scoped (switching chips must not pause a run you navigated away from). Pinned by `tests/test_multi_instance.py` (ownership matrix vs a real spawned process) + `tests/test_scheduler_scope.py`.

**Polling under a concurrent writer (docs/80).** Scan reads use `safe_io.scan_json` (one attempt, no retry ladder — a mid-write run just isn't finished yet, retrying it is pure latency). A present-but-unreadable file marks the run `incomplete`, forcing re-parse without defeating the date-dir short-circuit for its siblings. `/datasets/changes-since` isolates each folder (one raiser can't 500 the whole poll), holds a failed folder's cursor, has a wall-clock budget reported as `partial`/`skipped`, and can only return 200; the client aborts at 10s and backs off on failure. Pinned by `tests/test_poll_stability.py` (real folders, real truncated JSON, a live background writer) + `tests/dataset_poll_selfcheck.cjs`.

**Tier-2 scale batch (docs/105, docs/104 items #1–#11).** The poll budget reaches inside a folder with continuation semantics; a force_rescan's content-equal re-parses keep their old `last_parsed` so delta polls ship only genuinely changed rows. `_current_mtime` is deliberately NOT time-memoized (a TTL memo broke the pinned write-then-poll contract). The 50k-dir discovery cap renders an honest line. Working-copy GC unions in every live peer window's open chip. Default delta-poll interval 60→15s. **Live-Edit cold-column hydration**: over 4,000 cells, the client detaches td contents beyond ~2.5 viewports (server render unchanged; search stays whole-chip from a stored value map); below the gate, byte-identical. Pinned by `tests/test_poll_scan_budgets.py`, `tests/bulk_virt_selfcheck.cjs`, `tests/test_param_history_filter_ux.py`, `tests/test_multi_instance.py::TestGcPeerProtection`.

### Interactive Data (ndview + click contracts)

The dataset detail page's Data tab is `core/ndview.py` + `web/static/ndview.js`: a generic, crash-free N-D viewer over every variable of every `ds_*.h5` file — dual-format (`_open_reader`, r13/docs/67: NetCDF-classic via scipy, everything else via h5py with `DIMENSION_LIST` deref), name-based dim classification, peak-preserving decimation with click-snap, byte-aware budgets, bytes-caching LRU. `/dataset/<uid>/ndview[/data]` always returns HTTP 200 with classified fallbacks.

**A shot index is not an axis (docs/82).** `_default_view` used to order sweeps by size, and a repetition axis is always the biggest dim in the file, so shot-count dims were being plotted as if they were sweep axes. `_SHOT_DIM_NAMES` (curated from the archive's real names) gives kind `shot`, which is averaged away server-side whenever there is anything else to plot against, and kept only when there is nothing else (e.g. `iq_blobs`' per-shot scatter). Never silent — the controls strip states `averaged over n_runs (2,000 shots)`. Pinned by `tests/test_ndview_repeats.py` + `ndview_selfcheck.cjs`.

The Interactive tab's figures carry **click contracts** (`core/interactive_plots/contracts.py` + `recipes/`): each recipe bakes the calibration node's own update formula into the figure's `clickable` block, so a click stages the node-update value, not the raw coordinate. Round-trip goldens (`tests/test_click_contracts.py`, `tests/test_cz_contracts.py`) pin click-at-fit-optimum against the node's own `patches[].value` on real archive runs. `core/click_targets.py` ranks ndview write-candidates (absolute axes only, name-bound). Everything stages through `_openPlotApplyPopup` → `/field/peek` → `/field/edit[-batch]` with the chip-identity gate. See `docs/48_ndview_click_contracts.md`.

**The Interactive panel's four instabilities (docs/118).** A full-page-opened run's Interactive tab was rendering into the wrong pane ID (`#table-pane` vs `#inspector-pane`) so `switchDatasetTab` silently changed nothing; fixed, plus a `ResizeObserver` at both mount points, an offscreen-preferred (not visible-preferred) tile-purge policy, and pin/unpin round-tripping through markup without leaving loaded plots as unrevivable corpses. **One comparison rule**: `differ.compare_equal` is now the single source of truth for both the dataset diff's row-level "is this a difference" decision and the template's cell-level highlight (previously `100` vs `100.0` was flagged as different with nothing highlighted, and `nan` vs `nan` highlighted as a change). Pinned by `tests/test_compare_equality.py` + `tests/interactive_stability_selfcheck.cjs`.

### Pulses Page

**Pulses** (sidebar, nested under Live State Edit) is the pulse management surface: every pulse on the chip in one sortable table with server-side SVG sparklines, a detail inspector with instant per-field commit + a live waveform preview (debounced `/api/pulse/synth`), full-catalog create, and delete/rename/duplicate with reverse-pointer (`used_by`) safety. The preview is computed in-process by `core/waveform_synth.py`, pinned bit-for-bit against the customer env by `tests/test_waveform_golden.py`. `core/pulse_catalog.py` is the single source of truth for class schemas/defaults; `core/pulse_index.py` does enumeration + reverse-pointer indexing + pointer rewriting for duplicate/rename. See `docs/40_pulses_page.md`.

**Inline-commit stability (docs/75).** A field commit re-renders the whole inspector pane, which removes the focused input mid-commit — armored with a document-level `submit` listener that prevents the resulting native form fallback, plus `window.InlineCommit` restoring focus/caret/scroll across the re-render (matched by dot-path, never DOM index). Pinned by `tests/pulses_commit_selfcheck.cjs` + `tests/test_pulses_commit.py`.

### Generate Config + Config Viewer

`core/config_generator.py` drives the **Generate Config** wizard: discovers conda envs, probes for the QM stack, spawns `generator/run_build.py` in a user-selected env, and reads the resulting `_result.json`. The State Manager process never imports the heavy QM libraries. Both runners share `_run_script_outcome()`; a Preview config button stashes a result transplanted onto the store on load (seed-on-load).

After `machine.save()`, a post-save fix-up rewrites each readout MW input port's `downconverter_frequency` as a JSON pointer to its paired MW output port's `upconverter_frequency` (they share one physical LO).

The Populate step is absolute-first: RF frequencies are the primary input, LO auto-derived per MW-FEM port pair by `solveLoWindow` (minimize max|IF| subject to the ±400MHz window + Nyquist coverage + the 5MHz resonator demod hole). A Power input toggle adds an absolute-dBm mode (port `full_scale_power_dbm` auto-allocated; stored spec representation identical in both modes).

The r3 feedback batch (`docs/53_generate_feedback_r3.md`) added: as-you-type inline validation, CZ automatic control/target orientation by RF frequency, user-settable qubit naming schemes, a default-value presets archive, and editable Python build-script export (`core/script_emitter.py`).

**Auto-allocate is the default; the Wiring step delivers its diagram on its own (docs/134, 2026-08-22).** User directive after the CQT 20Q chip showed "a dead Auto-allocate button and a list, no diagram" (the `/instrument` "Modify wiring…" deep link into step 5). Three stacked defects, all in `generate.js`: ① the regenerate hydrate nulled an already-selected `state.env`; ② a no-env press answered only in the out-of-view `#gen-message` (every outcome now also writes `#gen-allocate-status` at the button, with four honest diagram placeholders incl. "No usable Python environment" once probing rules every env out); ③ `deriveLines` invented a flux line per qubit — the chip flux-biases 9 of 20 and the allocator ran out of DC channels before any diagram could render. Now: the first env that probes usable is auto-picked **client-side only** (`/generate/select-env` persists machine-wide + rebinds the open chip's type policy — never from a page view; the allocate dry run carries `python` in its body, a build persists the pick at that explicit act, a user click claims the selection synchronously and persists as before); step-5 entry **auto-runs** `/generate/allocate` when the allocation is absent or stale per the allocator-relevant `topoSig()` (entities + derived lines + pins + FEMs + gate + QDAC + TWPAs; pair ids normalized so a czAutoOrient flip never re-runs; deliberately not per-line coverage — shared-xy CR lines legitimately allocate nothing). **A response is bound to its request**: `_allocRunSeq` token + request-time sig stamp + `resetAllocRuntime()` at every content swap (the same-day 27-agent review's CRITICAL: a mid-flight edit was certified current by its own stale response, and chip A's allocation could land in chip B's hydrated wizard); the sig rides the session draft (`allocSig`). Failed auto attempts latch per failing input (`_allocFailSig`) and re-arm on any fix; the manual button always works. Regenerate derives optional lines (qubit flux z, pair coupler/CR) only where the SOURCE chip had them (`state.regenLineInventory`, frozen from the incoming spec BEFORE `applyDraft`; qubit known-ness = has lines, pair known-ness = entity list so a line-less pair never gets a coupler invented; keys sorted-within-pair, rename-remapped) — with explicit acts overriding: an architecture switch derives the new gate's lines ungated, the QDAC checkbox edits the inventory, and `regen_spec` now inverts a docs/119 chip's QDAC bias into `spec.qdac` (before: silently rebuilt with no bias at all). `init()`/`resetWizard` reset mode/endpoint/sourcePath (regen mode used to leak into a later Generate mount and post to `/regenerate/build`). Review: 23 raised, 22 confirmed, all fixed. Verified live twice: zero-click diagram in 8 s on the real chip, source pins respected. Pinned by `tests/generate_autoalloc_selfcheck.cjs` (80 executed asserts, 14-mutation-checked) + `tests/test_generate_autoalloc.py` + `TestQdacInversion`.

**QDAC-II bias (docs/119).** Some labs flux-bias qubits from an external QDAC-II DC source instead of an OPX LF-FEM port, via the customer's own `quam_config.qdac_components` (imported directly when present). Everything is degrade-only — a missing module or failed trigger allocation leaves that qubit with no z/bias component and a warning, never a crash. The trigger wiring is a second, isolated `Connectivity` (QDAC's custom `"qt"` wiring-line type isn't in the whitelisted builder's vocabulary). `_apply_qdac` reassigns the qubit's runtime `__class__` in place before `apply_populate` runs. Verified end-to-end on a real 20-qubit chip. Pinned by `tests/test_capabilities.py`, `tests/test_config_generator.py::TestValidateSpecQdac`, `tests/test_generate_qdac.py`, `tests/test_generate_qdac_live_build.py`.

`core/config_view.py` is the **Config Viewer**: shows the QM config dict + per-pulse waveform plots via the same subprocess pattern. Cache is staleness-checked via `meta["basis_hash"]` vs `working_copy.content_hash`. See `docs/27_config_generator.md`, `docs/30_config_viewer.md`.

### Re-generate Config

**Re-generate config** (`/regenerate`) lets a user take a chip they already built, edit its structure in the wizard, rebuild it fresh, and keep every calibrated value, plus emit a single editable Python build recipe. Pipeline (`core/regenerate.py`): `regen_spec.reconstruct_spec` inverts the chip → spec → wizard edits → `config_generator.run_generator` builds fresh structure into a NEW folder → `regen_merge.merge_states` carries calibrated values (tier-1) + grafts user-added subtrees (tier-2) → `regen_script.emit_build_script`. The State Manager process never imports quam/quam_builder.

Entity collections are never resurrected. TWPAs build natively via `Connectivity.add_twpa_lines` (legacy 0.2.0 builders fall back to a wiring-carry graft). Pair ids are reconciled by (control, target) membership at merge AND at build-time populate-seed lookup. A **cross-generation schema gate** refuses to graft an old-only key into a class the fresh build's own dataclass schema doesn't recognize (visibly dropped as `schema_dropped`, never a silent `Quam.load()` crash).

**A pair reference is a POINTER, and it can be TWO hops (docs/118).** A modern quam_builder chip's pair control/target reference is `#/wiring/qubit_pairs/<p>/c/control_qubit` → `#/qubits/qX`, not the one-hop form the old code assumed — every pair was silently dropped from the reconstructed spec, losing 59% of pair calibration on a real 10Q chip while the build still reported SUCCESS. `qubit_ref_name` now walks the chain one hop at a time correctly. Pinned by `tests/test_regen_pair_pointers.py`.

**Adaptive reconstruction + populate-protect (r16, docs/72)**: `reconstruct_spec` unions the declared `ports` inventory into the FEM set, tolerates explicit-null channels, and the merge accepts `protect_paths` so in-wizard Populate edits are never reverted by tier-1. Transparency counters surface in the build-result panel. An exact-spec sidecar at `<out>/.regen/generate_spec.json` is preferred on a later re-generate. See `docs/51_regenerate_config.md`.

### Environment Capability Validation

A build only works if the selected env exposes the exact functions the chip needs, and package versions lie (a lab's `quam_builder 0.2.0` may still have `add_twpa_lines` via a backport). `generator/probe_capabilities.py` runs in-env introspection (`import + hasattr`, stdlib-only at import); `core/capabilities.py` owns `REGISTRY` + `required_capabilities(spec)` + `assess(spec, manifest)` sorted into blocker / degrade / ok buckets. `/generate/build` + `/regenerate/build` enforce server-side. Pulse-class locators are multi-home (quam → quam_builder arch → quam_builder common) since quam 0.6.0/quam_builder 0.4.0 moved several classes out of `quam.components.pulses`. See `docs/52_env_capabilities.md`, `docs/53_qop37_alignment.md`.

### Type Coercion Philosophy

`modifier.py` casts new values to the original field's type but **never validates ranges**. Real QUAM data has coupler amplitudes >1, negative T2, angles outside [-π, π] — trust researcher input.

## Key Files

| File | Purpose |
|------|---------|
| `quam_state_manager/core/safe_io.py` | Conflict-safe live-file I/O — share-delete reads, `ReplaceFileW` writes (with an `os.replace` fallback on WinError 50/1 — WSL's P9 server doesn't implement ReplaceFileW), mtime-bracketed pair read, atomic JSON writes |
| `quam_state_manager/core/working_copy.py` | Per-chip working copy; sync/apply between it and the live files |
| `quam_state_manager/core/pointer_resolver.py` | Core innovation — resolves QUAM JSON pointers; cache lives per `QuamStore` |
| `quam_state_manager/core/loader.py` | QuamStore: loads via safe_io, merges, validates state+wiring; owns the per-instance pointer cache |
| `quam_state_manager/core/scanner.py` | Workspace discovery (safe_io-routed), LRU cache |
| `quam_state_manager/core/search_index.py` | <1ms keystroke search (prefix map + trigram) |
| `quam_state_manager/core/query.py` | Flattens nested JSON into qubit/pair property dicts |
| `quam_state_manager/core/modifier.py` | Edits with undo + two-phase rollback; clears the store's pointer cache on mutate |
| `quam_state_manager/core/saver.py` | Atomic writes (delegates to safe_io), `.bak` rotation, CSV/MD export |
| `quam_state_manager/core/pulse_catalog.py` | Pulse-class registry (params/defaults/inferred-length math) — single source of truth for Pulses page + add-pulse forms |
| `quam_state_manager/core/pair_columns.py` | Live State Edit pair grid — derives editable columns from the chip's real pair leaves, lab-flexible. Cells resolve through the same `_build_bulk_cell` pipeline as qubits; UI in the isolated `web/static/pair-edit.js` |
| `quam_state_manager/core/waveform_synth.py` | In-process numpy+scipy waveform synthesis (live preview; golden-pinned to the customer env's quam) |
| `quam_state_manager/core/pulse_index.py` | Pulse enumeration + reverse-pointer (used_by) index + duplicate/rename pointer rewriting |
| `quam_state_manager/generator/run_waveform_golden.py` | Env subprocess: dumps quam ground-truth waveforms → `tests/golden/waveform_golden.json` |
| `quam_state_manager/core/differ.py` | 2-way diffs, float tolerance, N-way trends |
| `quam_state_manager/core/history.py` | Param History: snapshots + SQLite trend index, chip fingerprints, alignment scan, migrations |
| `quam_state_manager/core/qualibrate_config.py` | READ-ONLY `~/.qualibrate` reader: env-var + merge fidelity, Win→WSL path dialect, projects listing, doctor lint, tray badge status |
| `quam_state_manager/core/dataset.py` | DatasetStore: discovers + indexes experiment runs; per-file h5py lock |
| `quam_state_manager/core/ndview.py` | Data-tab N-D cube builder — DIMENSION_LIST axis truth, decimation, byte-budget, bytes LRU |
| `quam_state_manager/core/click_targets.py` | ndview click→write-target candidates (absolute-axis-gated, dim-name-bound) |
| `quam_state_manager/core/interactive_plots/contracts.py` | Click-contract math — patches-first pre-update anchors, RF-at-run, increment/assign/wrap semantics |
| `quam_state_manager/core/interactive_plots/registry.py` | Recipe dispatch (two-tier name matcher) + bundle-input LRU |
| `quam_state_manager/core/experiment_data.py` | Loads node.json + data.json, extracts metadata/fit results |
| `quam_state_manager/core/config_generator.py` | Generate-Config wizard: env discovery, probe, subprocess runner |
| `quam_state_manager/core/config_view.py` | Config Viewer: cached `generate_config()` preview + waveform plots |
| `quam_state_manager/core/regenerate.py` | Re-generate orchestration: reconstruct → subprocess build → value merge → recipe emit + exact-spec sidecar |
| `quam_state_manager/core/regen_spec.py` | Invert a chip → build spec (pinned wiring, populate via pointer chains); exact-spec sidecar read/write |
| `quam_state_manager/core/regen_merge.py` | Value-preserving merge (tier-1 carry / tier-2 graft), pair-id reconciliation, redundant-op prune, TWPA wiring+ports carry |
| `quam_state_manager/core/regen_script.py` | Emit the single-file editable Python build recipe (generate + populate combined) |
| `quam_state_manager/core/capabilities.py` | Env-capability model: `REGISTRY` + `required_capabilities(spec)` + `assess` (blocker/degrade/ok buckets) |
| `quam_state_manager/generator/probe_capabilities.py` | In-env detection catalog (`CATALOG`/`CATALOG_IDS`) + `detect()` → capability manifest |
| `quam_state_manager/generator/run_build.py` | Standalone QM-stack subprocess script (allocate / build modes) |
| `quam_state_manager/generator/run_generate_config.py` | Standalone QM-stack subprocess script (config preview) |
| `quam_state_manager/web/routes.py` | ~90 Flask/HTMX routes |
| `quam_state_manager/web/static/app.js` | ~5,100 lines of vanilla JS for UI polish |
| `quam_state_manager/web/static/generate.js` | ~3,000 lines for the Generate-Config wizard frontend |
| `quam_state_manager/cli.py` | 10 Typer + Rich CLI commands (`--version`, `--json` for scripting) |
| `quam_state_manager/main.py` | pywebview desktop launcher |
| `build/quam-manager.spec` | PyInstaller onedir bundle config |
| `docs/00_overview.md` | Architecture deep-dive — start here |
| `docs/28_conflict_safe_io.md` | Working-copy + armored I/O (read this before touching file paths) |
| `docs/32_red_team_phase_2.md` | Phase 2 red-team findings + which are fixed |
| `docs/53_qop37_alignment.md` | Modern-stack (quam 0.6.0 / quam_builder 0.4.0) ground truth: class moves, `_PULSE_HOMES`, padding_length rename, ParametricCZGate gating — read before touching pulse-class or build-script imports |
| `docs/78_runner_agent.md` | Runner + AI calibration agent — the one-button bring-up loop. **Read §17 (audit) before trusting any earlier section**: it lists what shipped, what is still open, and which earlier claims were false |
| `quam_state_manager/core/autofit/envmatrix.py` | (env × analysis-tree revision × run generation) compatibility probe — the triple a replay verdict is only valid within |
| `quam_state_manager/core/autofit/sourceroot.py` | Read-only `git archive` materialization of a pinned analysis revision (never a checkout — the customer tree is untouchable) |
| `quam_state_manager/core/autofit/figure_gen.py` | Regenerate a run's figure through the LAB's own plotting module, with a wrong-fit injection seam |
| `quam_state_manager/core/autofit/judge_pack.py` | Vision-judge family knowledge: versioned JSON pack + the Clause-B lint enforced at LOAD (a bad exemplar is dropped, never taught) |
| `quam_state_manager/core/autofit/verification.py` | The context a verdict is only valid inside — ONE shape stamped by `fit_audit`, `figure_gen` and the engine; `comparable()` is what stops two gate revisions being read as one |

## Tests

Tests use `tmp_path` synthetic fixtures. Some tests additionally probe real data from `<data-root>\...` and auto-skip when that path is absent. No test config files — pytest uses default discovery. An autouse fixture in `tests/conftest.py` points `QUALIBRATE_CONFIG_FILE` at a nonexistent tmp path so the suite never reads the developer's real `~/.qualibrate` (docs/63). The qualibrate/project-scope test files also run green on native Windows — their fixtures interpolate TOML paths `as_posix()` (raw backslashes are escapes inside TOML basic strings and silently void the whole config). See the Commands section above for the current `cqt`-env baseline — re-derive it in whatever env you use rather than trusting a number from a different or since-removed env.

## Dependencies

Core: Flask, Jinja2, Typer, Rich. Desktop: pywebview. Data: h5py (HDF5 reading), numpy + scipy (Pulses-page waveform synthesis — scipy gives bit-exact window/filter parity with qualang_tools). Frontend: HTMX, Pico CSS, Split.js, Plotly.js — all bundled in `web/static/` (no CDN). The Generate-Config wizard shells out to a user-selected conda env that has the heavy QM stack (`qualang_tools`, `quam_builder`, `quam`, `qm`) installed; the State Manager process itself never imports them.

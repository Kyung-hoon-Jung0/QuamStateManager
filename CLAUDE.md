# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Note on this file's history**: this file previously accumulated long forensic write-ups of past debugging campaigns (reproduction steps, measured timings, "the first fix was wrong because..." narratives). It was trimmed on 2026-08-21 to keep only current contracts/invariants + doc pointers — the full history for any `docs/NN` reference below lives in `docs/NN_*.md`. Read the doc file before touching related code if you need the "why."

## Project Overview

**QUAM State Manager** is a desktop + web tool for inspecting and editing quantum machine (QUAM) state files. It reads `state.json` + `wiring.json`, resolves custom JSON pointer references, and provides a CLI, Flask web UI, and pywebview desktop app. It also generates fresh QUAM configurations (Generate Config wizard) and previews `machine.generate_config()` output (Config Viewer).

## Commands

**Run tests** (Windows conda env **`cqt`** — the canonical pytest env since 2026-08-16, user-directed. It carries the full customer QM stack — quam 0.6.0 / quam_builder / qualang_tools / qm 1.4.0 plus the customer's own `quam_config` — so it runs the plain suite AND the QM-subprocess tests other envs skip; its stdout carries a `qm - INFO - Starting session` line, filter it when parsing. Conda envs here churn — confirm with `conda env list` rather than trusting any name. `PYTHONUTF8=1` is REQUIRED: the node-selfcheck drivers' subprocess output is UTF-8 and Windows' default cp949 codec otherwise nulls their stdout. `--timeout` keeps any hang from stalling the suite silently):
```bash
PYTHONUTF8=1 conda run -n cqt python -m pytest tests/ -q --timeout=900 --timeout-method=thread
# Single test file:
PYTHONUTF8=1 conda run -n cqt python -m pytest tests/test_pointer_resolver.py -v
```
**Expected `cqt` baseline (docs/127, 2026-08-21): 14 failed, ~5,750+ passed, 247 skipped**, with `--deselect tests/test_main.py::TestWaitForServer`. The 14 are an OS-behavior-difference class documented in `docs/87` (loopback/WSL-kernel checks, tmp-path case-identity tests, a couple of Windows file-locking timing flakes that pass in isolation) — not regressions. `test_autofit_gates`/`test_autofit_synth`/`test_runner_p2` failures (23 of them, `ndarray.ptp()` removed in numpy 2.x) were fixed in docs/127; if they reappear it's a real numpy-2.x incompatibility, not environmental. **Re-derive this baseline in whatever env you actually use** rather than trusting old numbers from other envs (`LabD_17Q`/`qm_mng` no longer exist on this machine).

**The `.cjs` selfchecks need node + jsdom — and they SKIP, loudly-looking-green, without them.** Many pins in `tests/` drive the real shipped JS under jsdom via a Python driver whose gate is `pytest.skip("jsdom not installed")`. Run `npm install` (jsdom + canvas) before trusting a "clean" run. `npm run selfcheck` reports `(N assertions)`, `(silent style)` (legit — `ok()` only speaks on failure), or `ok? … (exit 0 but NO output)` (can't tell asserting-nothing from passing). The harnesses hand Node individual globals (`global.window`, `global.document`, …) rather than a real jsdom script realm — a guard that checks `window.X` while the code *calls* bare `X` throws instead of degrading (the `CSS` global bug, docs/78-era). When adding a harness, bridge every global the code under test reads bare.

**Run web dev server** (use a non-default port — stale dev servers stack up; serve from a Windows env, never WSL — broken loopback forwarding on this machine):
```bash
conda run -n LabD_17Q python -c "from quam_state_manager.web.app import create_app; create_app().run(debug=True, port=5050)"
```

**Run CLI:**
```bash
python -m quam_state_manager.cli show qA1 -f "path/to/quam_state/"
python -m quam_state_manager.cli --version
python -m quam_state_manager.cli --help
```

**Run desktop app:**
```bash
python -m quam_state_manager
```

**Build standalone executable:**
```bash
pyinstaller build/quam-manager.spec
# Output: dist/quam-manager/quam-manager.exe
```
Uses **onedir mode** (not onefile) for instant cold start — onefile extracts to temp on every launch (3–10s overhead).

## Architecture

### Core Pipeline

```
state.json + wiring.json
    → safe_io.read_state_wiring (FILE_SHARE_DELETE + mtime-bracketed pair read)
        → loader.py (QuamStore — owns per-instance pointer cache)
            → pointer_resolver.py (resolves #/, #../, #./ references via store cache)
            → search_index.py (prefix map + trigram index)
            → query.py (flattens nested JSON → qubit/pair dicts)
            → modifier.py (type-coerced edits, undo, rollback)
            → saver.py (atomic writes via safe_io.atomic_write_json, .bak rotation)
            → differ.py (2-way diffs, N-way trends)
            → history.py (Param History: SQLite property index, snapshots)
            → config_view.py (Config Viewer — preview generate_config())
            → config_generator.py (Generate wizard subprocess driver)
```

### QUAM Pointer System

QUAM files avoid value duplication via three pointer types:
- `#/qubits/qA1/f_01` — absolute from root
- `#../x180/length` — relative to parent
- `#./self_ref` — relative to self

`pointer_resolver.py` resolves these **on-read, never in-place**. The cache is **per-`QuamStore`** (each store owns its own dict + lock), so two chips loaded in the same process with same-named qubits never share resolutions. Unresolvable pointers return raw strings + log warnings (real data has dangling pointers). See `docs/32_red_team_phase_2.md` finding 0.1.

### QuamStore (Thread-Safe)

`core/loader.py` holds merged state+wiring dicts, change log, search index, and per-store pointer cache in a single `QuamStore`. All mutations are guarded by `threading.RLock` (Flask workers + pywebview UI thread share state). Loads route through `safe_io.read_state_wiring` so the read never blocks an experiment's atomic save on Windows.

### Working-Copy / Live-File Safety

Experiment programs read and write the same `state.json` + `wiring.json` the app watches; on Windows a plain `open()` for read blocks their atomic `os.replace` save. So the app operates on a **working copy** under `instance/working_state/` (`core/working_copy.py`): the `QuamStore` and `Saver` are built on the copy, background change detection is `os.stat`-only, and the live files are read only on an explicit user **sync** and written only on an explicit **apply to live**. All live-file content I/O goes through `core/safe_io.py` (share-delete reads, `ReplaceFileW` writes, transient retry, mtime-bracketed pair read). LRU eviction in `_activate_quam` keeps the on-disk working folder so a re-load rehydrates unapplied edits via `working_copy.load`. See `docs/28_conflict_safe_io.md`.

**State-roundtrip invariant (docs/65):** content loaded WHOLESALE into the working copy (dataset Load State, State-History stage, Revert last apply) has no change-log entries and a cleared re-apply stash — `/state/sync` must never pull-first over it. A `ctx["staged_base"]` marker overrides the carve-out so staged+edited mixed state still pushes whole. Revert-last-apply's fallback target is content-matched, never the newest snapshot. Pinned by `tests/test_state_roundtrip.py` + `tests/state_sync_selfcheck.cjs`.

**SM never swaps what you are looking at (docs/87).** `reconcile_with_live` no longer auto-pulls on a provably-clean working copy from the user-facing path (`sync_if_clean=False`) — a stale-chip bug's fix used to silently overwrite whatever the user was looking at, which is worse than the staleness it fixed. `auto_adopt` splits by ACTOR, not behavior: the user opening a chip gets `False`; the scheduler's post-node hook and autofit's `reconcile()` get `True` (a robot mid-loop doesn't stop to ask, and autofit's gates need a live anchor). A run started from SM's own Experiment Runner still adopts silently; only writes from OUTSIDE SM raise the drift banner, which now reports "N values differ" from data already read for the verdict (zero added live reads). Pinned by `tests/test_live_replace_routes.py` + `tests/test_sync_robustness.py`.

**A field the entity has not got yet (docs/88).** A cell edit into a leaf whose parent is itself an unresolved pointer used to try to walk into the pointer string. `edit_policy.resolve_missing_leaf_path` only fills in a truly-missing final key (never infers into a list parent); creation stays declared-only via the existing `create` flag, which the grids set from the server's `data-missing` marker. Pinned by `tests/test_missing_leaf_edit.py`.

**The third choice when live drifted (docs/86).** When something outside SM rewrites the live files, the user can now choose a direction instead of only pulling: **↑ Keep mine — overwrite live** sits beside Pull/Sync in both the drift banner and the review modal, gated to skip archives, with a preflight (`GET /state/overwrite-live/preflight`) naming what disappears and one forced confirm (pull and push are NOT symmetric — a pull discards nothing on disk, a push discards live content). A provably-clean working copy still auto-pulls silently (unrelated to this — that's the stale-chip case docs/87 fixed). **One directional-trio vocabulary (docs/97):** every surface now labels the same three actions identically — ↓ Take live / ↑ Keep mine — overwrite live / ⇄ Pull & apply (merge) — instead of six different historical labels. Pinned by `tests/test_overwrite_live.py` + `tests/state_sync_selfcheck.cjs`.

The **load path is content-hash-aware**: each sync point records `synced_live_hash` in the meta sidecar, and `reconcile_with_live` runs on load/select — live replaced + working copy provably clean ⇒ auto-pull; replaced + possibly edited ⇒ keep + `live_diverged` banner (never clobber); unreadable live ⇒ never treated as replaced. Working-copy **GC** deletes only provably-clean copies (transient read failures classify as kept-unverifiable), and its in-use set folds in every LIVE peer window's open chip via `instances.peers()` + `working_copy.key_for`.

### Scanner + LRU Cache + Sidebar Tree v2

`core/scanner.py` discovers experiment folders lazily. `QuamStore` instances are LRU-cached (max 10, ~40MB total) — only loaded when selected in the UI. `node.json` reads also go through `safe_io.read_json`.

**Sidebar tree v2 (r13, docs/68):** `Workspace.tree` stays the flat `{root: [DateGroup]}` data model; `build_nested_tree(root, entries)` derives a VS-Code-style render model per root (real folder levels, date levels descending, leaf entries run-id descending with the 50-cap keeping the newest 50). Staleness = a structure-spine probe: per root, the recorded set of run-parent dirs + ancestors is re-statted as a `{dir → mtime}` map. `rescan_root` scans outside the lock and swaps the tree in one rebind. The active run's ancestor `<details>` get `.tree-branch-active` so a collapsed tree still shows where the open experiment lives.

### Web UI (HTMX)

Flask routes return either full pages or partial `_*.html` template fragments for `hx-swap`. Partials are named `_*.html`; full pages are named without underscore. No React/Vue — HTMX + ~5,100 lines of vanilla JS + ~3,000 lines for the Generate-Config wizard. Routes in `web/routes.py` (~3,800 lines, ~90 endpoints). Templates: 63 files (17 full pages + 46 partials). All colors are parameterized as ~430 CSS custom properties with light/dark theme support.

Clicking a Plotly data point in a dataset figure opens a confirmation popup (one row per affected dot-path, editable, per-row Apply + Apply All) rather than auto-applying. See `docs/36_plot_click_confirm_popup.md`.

**Δ everywhere (docs/76).** Every before→after surface renders old/new/difference through ONE implementation: `core/value_delta.py` (+ the `value_delta` Jinja filter, `_delta_macros.html`) server-side, `window.ValueDelta` client-side — pinned character-for-character (`tests/test_value_delta.py`). Subtraction is exact decimal; a Δ renders only where it means something (booleans/nulls/pointers/strings/created-deleted subtrees render nothing, never a fabricated 0). Wired into essentially every diff/compare surface in the app (Review tray, sync screen, plot-click popup, Prev-State diff, Compare-selected, value/Column History, autofit ledger, `/changes`, undo toast).

The qubit/pair inspector panes carry a sticky in-panel search bar (`filterDetailPanel` in app.js — AND-tokens across all `<details>` sections, auto-opens matches, "X of Y" count).

**The diff workbench is the front door (docs/84).** `/diff?a=<ref>&b=<ref>&tab=state|wiring|node|data&view=tree|list` is the one comparison surface (tree = the Explorer's `renderJsonTree` with a diff overlay; "Differences only" is a pruned document via `core/json_diff.py`, ranked by |Δ%|, pruned to changed leaves + ancestors). Every legacy comparison entry point (`/compare`, `/chip-compare`, sidebar Compare) redirects into it; the Compare hub sits behind "Advanced…" for entity mapping + N-way baskets. Pinned by `tests/test_diff_workbench.py` + `tests/diff_tree_selfcheck.cjs`.

**One search grammar (docs/96).** space = AND, standalone `|` = OR (tight binding), every other pipe literal. `web/static/search-query.js` + `core/search_query.py` are JS↔PY parity-pinned; wired into tree search, bulk/pair grids, Datasets table, sidebar filter. **A token that NAMES a row wins the row axis (docs/110)** — both grids classify each token as column/id/value with exclusive precedence, so a qubit-id token that also happens to appear in a column's search text no longer gets swallowed as neutral. Pinned by `bulk_search_selfcheck.cjs`.

Comparison also lives in the **Compare hub** (`/compare-hub`) — one URL-canonical surface (`src=` ref tokens + `bucket`/`preset`/`ref`/`map`) with three declared contexts (same chip over time / same design different device / different devices). Engine = `core/compare.py` + `core/compare_sources.py` (isolated source pool). Snapshot-vs-snapshot for a single chip stays in Param History. See `docs/49_compare_hub_redesign.md`, `docs/35_chip_compare.md`.

**What actually leaves the instrument (docs/109).** Every amplitude on the entity surfaces shows its true physical output: MW channels show dBm via the FSP identity `P = FSP + 20·log10|amp|`, flux/LF channels show volts. `core/physical_units.py` (pure): `channel_of` walks ≤4 ancestors to the port dict. Honest by construction — text values, MW amp 0, dangling chains, no channel ancestor ⇒ blank, never invented. Surfaced as a muted sub-line in bulk grid cells + the inspector's `phys_amp` filter. Pinned by `tests/test_physical_units.py`.

**A tab keeps its state; the grid works like a spreadsheet; failures explain themselves (docs/110–115).** A seven-workstream daily-flow campaign. **PaneState (docs/110)**: main-pane keep-alive across htmx nav — parks the outgoing pane's DOM at `beforeSwap`, restores at `afterSwap` when the tray's `mutation_seq` + chip token both match, else the fresh render stands (never cancels the htmx request itself). **docs/111** adds same-column multi-select, Ctrl+D fill-down, multi-line paste as one undo action, pinned columns, dyn-column reload that carries unsaved edits. **docs/112** adds j/k/Enter/Space/Escape run navigation + a digest band over the filtered set. **docs/113** ships `/` focus-search, Ctrl+Enter on armed Apply-all, a `?` cheat sheet. **docs/114** makes failure honest: a non-chip folder offers every real subfolder, a dangling pointer says DANGLING, write permission is probed at open. **docs/115** gives the manual a permanent `/help` address and a real onboarding CTA. Pinned by `tests/test_pane_state.py`, `test_grid_editing.py`, `test_ds_flow.py`, `test_kb_polish.py`, `test_honest_failures.py`, `test_onboarding.py` + matching `*_selfcheck.cjs`.

**Live State Edit shows everything (docs/85).** Every curated and derived column defaults visible now (hiding was a rendering decision the search box didn't know about — `T1` was hidden while ~200 obscure derived leaves showed). `headline_on` is a separate flag from `default_on` for which pair ROWS Compare summarizes (kept byte-identical to the old formula under a new name). Search always scans every column regardless of visibility, with a hint chip to reveal hidden matches (never auto-reveals; hidden values never enter the search haystack). Persisted column choices are versioned (`…_cols_v2`) so the visibility flip reaches everyone. Pinned by `tests/test_bulk_edit.py` + `tests/test_pair_columns.py` + `tests/bulk_dyncols_selfcheck.cjs`.

**Settings + Calculator are sidebar tools (docs/89).** Moved from the far top-right to the first row of the sidebar (icon + label above a divider). Collapse traps handled: a compact icon-only pair shows only while the sidebar is collapsed (not a duplicate — `.sidebar-collapsed` is `width:0`, not an icon rail), and both popovers are body-level positioned in viewport coords (the sidebar's `overflow-y:auto` would clip an absolutely-positioned child). Icons are inline SVG so they inherit theme + UI scale. **Calculator (docs/100)**: formulas audited exact; a fourth RF=LO+IF section added, solver pure and DOM-free-pinned. Pinned by `tests/test_sidebar_tools.py` + `tests/sidebar_tools_selfcheck.cjs`.

**Chip topology as a picture (docs/92, plan docs/91).** `TopoGraph.layoutFor` centralizes three honest states: `physical` (only when every node passes a strict grid gate), `logical` (derived from pair connectivity alone, with a visible note), `none` (one honest line, never an invented map). Two maps, one division of labour: the **Chip Status hero map** carries numbers (per-node metric values, edge metrics, hover popup); the **component-page map** (`TopoGraph.renderLayout`/`ComponentMap`) carries no numbers — only structure (stones, pair edges, resonator marks, flux stubs, feedline buses), with map↔table hover binding both ways. Pinned by `tests/topo_graph_selfcheck.cjs` + `chip_status_hero_selfcheck.cjs` + `component_map_selfcheck.cjs`; verified on 8 real chips.

**The schema-drift healing chain + the silent grid cap (docs/94).** A lab's out-of-band class migration exposed three breaks in the healing chain (manifest cache key now folds in the class set; the docs/79 warm-carry now gates on class-inventory coverage, not just version; the three content-entry choke points now re-attach/re-probe). The Live-Edit qubit grid's dynamic-column cap was silently truncating real 452-column chips (`MAX_DYNAMIC_COLUMNS` 400→1200, and a tripped cap now renders a visible note). Pinned by `tests/test_schema_heal.py` + `tests/test_bulk_edit.py::TestDynColumnCapHonesty`.

**Every pulse class, generated and loadable (docs/98).** An all-pulse-class verification round caught two `Quam.load()` killers reachable from the UI: class-prefix acceptance now checks against the SELECTED env's own importable homes (not just catalog membership), and `pulse_catalog.env_field_filter` drops template fields the env doesn't know about before writing. A gate-slot pulse's leaves are now indexed per-leaf like `create_subtree`. Pinned by `tests/test_pulse_env_canonical_home.py`.

**Component-map feedback r1 (docs/93).** Five user items, each root-caused: chain-tab computation order fixed (a line-move, `tests/test_chain_tabs.py`); map grew 1.875× with id font derived from the cell as an SVG attribute (a stylesheet rule beats a presentation attribute — this project has hit that trap three times); active chain filter lights its stones; feedlines get identity colours on the resonators page (7 hues, dataviz-validated both themes, spatial slot order, per-slot dash as secondary encoding); frequency-inequality chevrons on pair edges (one per physical pair, gated on both-numeric + |Δ|≥1MHz). Pinned by `component_map_selfcheck.cjs` + `test_chain_tabs.py`.

**Sidebar IA (r15, docs/69):** structure→health order — Instrument Wiring · Chip Components subnav (Qubits/Pairs/Resonators/Flux/Couplers) · Diagnostics · Chip Status · Compare · Live State Edit ▾(Json Tree View, Pulses) · State History ▾(Param History) · Experiment Runner / Fit Replay / Auto Calibrate (routes `/scheduler` `/fit-audit` `/autofit`, page tokens unchanged) · Datasets ▾(Collections, Trends). Pins in `TestSidebarIAr15`/`TestActiveMarkingR15`.

UI state persisted in `localStorage`: `quam_font_size`, `quam_sidebar_collapsed`, `quam_tray_open`, `quam_split_sizes`, `quam_theme`, `quam_exp_list_compact`, `quam_qualibrate_nav_collapsed`, `recentFolders`, `quam_instrument_fit` (rack Fit/1:1, docs/135 — absent means "decide from the pane width"), `quam_chip_density_panels` (docs/141 §4o, per-panel tile size), `quam_bulk_qhidden:pairs:<chip>` (§4s, the Pairs picker's hidden set), `quam_bulk_fs` + `quam_bulk_col_widths` (the Live-Edit table-size slider and drag-resized columns — both feed the client's virtualization estimate, which is why the server's mirror is pinned, §4ac), and the Live-Edit column sets `quam_bulk_hidden_cols_v2` / `quam_bulk_hidden_cols_pair_v2` / `quam_bulk_dynhidden` (all HIDDEN sets — absent means show everything). In **sessionStorage**: `quam_sidebar_compare_sel` (§4y, the compare selection MODEL — not a projection of the tree, see §4ac).

Modal focus traps (`trapFocus` in app.js) are leak-proof at the source: re-trapping a container releases its previous trap, and a trap whose container went hidden/detached self-heals on the next keydown. Both Live-Edit grids hop Tab/Shift+Tab between edit cells; the topbar calculator hops its visible inputs. Pinned by `tests/tab_focus_selfcheck.cjs` + `tests/test_tab_focus.py`. See `docs/64_tab_focus_calc.md`.

**Customer feedback rounds 2–3 (docs/126, 2026-08-19) — real-browser verified against the PJ_10082026 20Q QDAC chip.** Highlights: `pair_columns` now mirrors `qubit_columns`'s port-chain expansion (on a QDAC chip the coupler, not the qubit, is wired to the OPX flux port — those fields existed on no grid before); topology hero metric patches, 2Q edge metrics, pane-width zoom; Live-Edit patch-render time cut ~2× (batched column hydration, one generated stylesheet instead of ~9,000 per-cell class toggles); Json Tree gained `ExplorerChips` port-owner chips + row-copy; **Apply-to-chip (round 3, user-directed) now applies over pending edits/live drift WITHOUT asking**, naming what happened in the result line — this is the current behavior, superseding the older docs/108/116 gate descriptions below for this one button; a floating Instrument Wiring panel; `CosineBipolarPulse` added to the pulse catalog; `core/gaussian_cz.py` transcribes the customer's own CZ-macro planner as one Pulses-page button. Auto-Sync pill/panel restyled, chip Versions panel relabeled with a quick-diff of the two newest snapshots, topology role-marker/metric z-order fixed, outlier scoring made spec-verdict-first (a `pass` value is never flagged), sidebar search debounce/sync fixed, and a printable Chip Status report (`GET /chip-status/report`) added. **#22 digital triggers (2026-08-20):** the Instrument Wiring diagram now scans every entity for `digital_outputs` (pointer chains followed, deduplicated per physical line) and renders a DIG sub-column per FEM; chips with no digital wiring render byte-identically. Pinned by `test_pair_columns`, `test_gaussian_cz`, `test_explorer_features`, `test_misc_ui`, `test_dataset_apply_to_chip`, `test_state_versions`, `test_report_card`, `test_instrument_digital`, plus matching `*_selfcheck.cjs` files.

**Versions panel: per-row Diff + differences-only N-way Compare (docs/128, 2026-08-21).** Every version row carries a read-only **Diff** button — this-version-vs-NOW in the sync review modal's shell + row language (engine: the pre-existing `HistoryManager.diff_current`; NONE of the sync flow's write actions ride along — Pull to Live stays the one gated write, and the note naming it renders only where that button actually exists). Tick-3+ Compare now lands on `/diff/versions`: one row per differing dot-path, one column per version, agreeing leaves never rendered (`Differ.diff_n` — every leaf, per-cell `changed` computed with the row verdict's own equality, docs/118); the Compare hub survives as that page's "Advanced" link. Both new routes pass `ignore_keys=set()` so a class migration (docs/94) is a visible difference rather than a silent one — the page TELLS the user every other leaf agrees, so it must not skip `__class__`; `diff_n` also treats two NaNs as agreeing (docs/118's rule, not a third answer). **Compact means adjacent, not narrow**: the row's actions render between the date and the meta (`margin-left:auto` removed) — the first attempt narrowed the panel 46→36rem instead and that measured as a regression, crushing the docs/126 quick-diff table it shares width with. Two adversarial review rounds (the second a 41-agent red team against the shipped commit: 34 raised, 4 confirmed + 1 from the completeness critic) found 14 defects, all fixed — notably `smModalOpen` not knowing the new overlay (global shortcuts fired behind the modal), a chip-identity gate for two-window races, a stale-response token, an N-way key column collapsing to one character per line, and sticky headers that never stuck. Pinned by `TestVersionDiff`/`TestVersionsCompareNway`/`TestCompactRows` + `tests/version_diff_selfcheck.cjs` (the JS pins were grep-only until the review; the selfcheck executes them and is mutation-checked).

**Versions answer WHO/WHY, not just WHEN (docs/132, 2026-08-22).** Customer round 2. Every row wears ONE of three kinds in the user's vocabulary — **EXP** (a run produced this state) / **MANUAL** (deliberate user change: apply, pull, restore, snapshot, bookmark) / **BACKUP** (protective pre-write copy) — via the additive `SnapshotMeta.kind` stamped at all 14 capture sites (raw `trigger` untouched; legacy snapshots display through the single `kind_for` mapping, flagged "likely reading", never claimed certain; `restore` finally has a label anywhere). EXP rows carry a clickable **After #\<run_id\>** chip (qualibrate saves state post-fit — "after" is the honest word) opening the run's data panel; **only where a run is recorded, never a time guess**. Recording is made real by near-real-time ingest: enqueue-only detection (delta poll aggregation / scheduler hook / a live-write mtime EDGE on the drift poll — two stats; the budgeted scan runs on the worker), a gated worker calling `HistoryManager.ingest_run` (fingerprint gate verbatim from `_runs_field_series`, chip decisions respected, mid-write retries, cross-process idempotent, REAL diff_summary), the scheduler hook REORDERED so ingest precedes the adopt (hash-dedup collapses the duplicate — attribution is never threaded through the docs/87 choke point), and **both orders converge**: identical content dedups forward, enriches (annotates run fields onto the existing row) backward. `hist_seq` rides `/state/drift` so open panels follow other-process captures (healing the old two-window staleness). Panel defaults to **changes-only** rows (zero-diff copies hidden with an honest count; pinned/EXP/current/labeled exempt) and states retention truth ("All N kept"); the current-version hash scan is O(1) now. **Per-value ✓ take** on all three compare surfaces (overlay / N-way / workbench gated to one-`working:`-side + state/wiring tabs) stages single values into the working copy via `/field/edit-batch` with `expect_chip` — live untouched until Apply. Dataset poll 15s→5s (pre-existing budget/backoff guards); new-run popup closes via ✕/Esc/backdrop. Pinned across `TestKindStamping`/`TestRunIngest`/`TestHistorySeq` (test_history), `TestKindChips`/`TestExpRow`/`TestChangesOnlyFilter`/`TestPanelFollowsIngest`/`TestPerValueTake`/`TestWorkbenchTake` (test_state_versions), `TestExpIngestOnPoll`/`TestSchedulerHookOrder` (test_poll_stability), extended `version_diff_selfcheck.cjs` (38 asserts; caught a real bare-`showToast` guard/call mismatch — the standing harness rule — the day it was written). A same-day 35-agent heavy review then confirmed 27 findings + 4 from the critic (one rejection in 28), all fixed — three CRITICAL: a double-drain race whose dedup branch deleted the just-created snapshot and poisoned re-ingest (drains now claim-and-serialize; ingest_run runs under the manager lock), double-quoted `tojson` attributes staging "" for every string take (single-quoted now, the documented Flask contract), and run-local wall-clock stamped into the UTC snapshot namespace (both stamp derivations convert LOCAL→UTC together). Full ledger in docs/132's review section.

**QDAC + LF-FEM on ONE qubit, through the LAB's own builder (docs/137, 2026-08-24).** docs/136 taught SM's own build path the bias-tee shape; this teaches the path the lab actually runs. Their `build_quam_qdac.py:93` picks one class per qubit id (`QdacBiasedFixedFrequencyTransmon if is_qdac_biased else machine.qubit_type`), the two guards around it are exact complements (a flux line on a QDAC qubit raises `:107`, a `qt` line on a non-QDAC qubit raises `:119`) so a qubit needing both is unreachable from either side — and `:131`'s unconditional `transmon.z = QdacBiasLine(...)` runs AFTER the line loop, **silently overwriting** the FluxLine on a qubit that did get one. No exception, a chip that saves, a flux line that stopped existing. `core/qdac_lf_recipe.py` emits two files into the existing recipe bundle, ONLY for a spec with a bias-tee qubit (a plain chip's bundle is byte-identical): `build_quam_qdac_lf.py` (a transcription of their `_add_transmons_with_qdac` with four named divergences — three-way class pick, both guards narrowed, and the bias landing on a field chosen by mode; everything structural is IMPORTED from their own modules, and `build_quam_wiring_qdac.py` needs zero changes because `set_nested_value_with_path` is a setdefault chain so `qt` lands as a SIBLING of `z`) and `generate_qdac_LF_combined.py` (three qubit sets, declared cabling, and **every** flux/coupler line explicitly pinned — their generator cables couplers by allocation ORDER with only the last 7 pinned, so adding one qubit flux line, which is exactly what a bias-tee qubit does, moves every unpinned coupler one port along with no error and no symptom until a CZ misbehaves). Transcribed rather than wrapped on purpose: delegating would mean stripping `qt` from `machine.wiring`, a QuamDict whose nested writes do not reliably stick. **The lab must add ~10 lines themselves** (`QdacBiasedFluxTunableTransmon` + the widened Union); SM emits the text in the README and both files RAISE at import without it — the opposite of SM's own degrade, because a hand-run generator makes one chip and a silently-unbiased qubit looks like a working one. The generator also loads back what it wrote before exiting (a forgotten Union otherwise fails only in the NEXT process, after `save()` overwrote the good file). **Three facts only a real run found**: the wirer indexes qubits by NUMBER (`qubits=["q1"]` is accepted and allocates nothing recognisable); the ext→port table must fall back to the allocation or `build_quam_wiring_qdac` dies with `KeyError: 'ext1'`; and a pair is `q1-q2` in a spec but `q1-2` in an allocation, so a verbatim lookup missed EVERY coupler — producing a chip with no qubit pairs at all while the generator printed "reloaded clean" (second time this spelling has bitten in two days; docs/136 §18 was the first). Fixed twice over: `_alloc_keys` tries every spelling, AND the emitted script checks `PAIR_COUPLERS` against `FLUX_PINS` and raises on a gap, because the resolution and the silence are separate defects. **Measured end to end** against a COPY of the PJ `quam_config` + the 10 lines (their tree untouched): emit → run → fresh-process `Quam.load()` → `q1.z = FluxLine(+const, opx_output)` AND `q1.qdac_bias = QdacBiasLine(ch13, ext1, triggered)`, both wiring entries in one qubit dict, `q1_qdac_trigger` in `generate_config()`, q2 untouched. Pinned by `tests/test_qdac_lf_combined.py` (**10/10 mutations caught, five of them breaking the LIVE build**, which is parametrized over a coupler-pair chip and a plain one; the pin-required check is STRUCTURAL because asserting the message wording alone passes with the guard deleted). Also fixed: the two `quam_config not importable` degrade strings claimed the biased qubits "have no z/flux component at all" (false for a bias tee, and the first returns before `_attach_qdac_bias` so it is the only thing said), and the build result reported `qdac_qubits` as the spec's INTENT when nothing got wired — reading as full success exactly when the path failed. **One review claim was rejected after checking**: the `want_tee and not has_z` degrade builds a valid loadable QDAC-only qubit, not the docs/136 CRITICAL shape. **Still open**: no lab has added the ten lines; `populate_quam_qdac.py` still keys on `isinstance(..., QdacBiasedFixedFrequencyTransmon)` in six places (only matters if they run their own populate on a tee chip); the trigger element is not `align()`-aware, which is already true of every QDAC qubit today.

**A 2Q RB number is per-Clifford or per-gate (docs/138, 2026-08-24).** Customer report: the topology headline said the q19-20 CZ was **97.1%** while the per-pulse sections on the SAME page said 99.09% and 99.34%. `StandardRB` is **1−EPC (per Clifford)**, `InterleavedRB` is **1−EPG (per gate)** — established three ways: arithmetically (`(d-1)/d·(1-α)` and `(d-1)/d·(1-α_int/α_ref)` reproduce both to 1e-12, d=4), from the lab's node docstrings ("fitted average **Clifford** fidelity" vs "fitted **CZ-gate** fidelity"), and from their `two_qubit_rb/fidelity.py:87` (`epg = epc/average_gates_per_clifford`; the node computes `average_gate_fidelity` and then stores only the Clifford one). On this chip `average_gates_per_clifford = 5.371` — a Clifford is over FIVE 2Q gates, so the two are nowhere near interchangeable. Three defects, one cause (`_extract_pair_gate_fidelities` emitted every numeric key as an undifferentiated fidelity row): ① `EDGE_METRICS` order sets the default, `cz_fidelity` came only from `Bell_State` so on an RB chip it was filtered out and the CLIFFORD metric became the default; ② `*_alpha` — the RB decay base — rendered as `95.62%` under a heading reading "Gate fidelity"; ③ the edge was grey on a pair characterised by interleaved RB, the better measurement. Fixed: `query._RB_LEVEL` stamps each row `gate`/`clifford`/`state`/`decay`; the edge takes `InterleavedRB` when there is no Bell_State (`fidelity_source="interleaved_rb"`, `best_gate` = the winning pulse) while **`StandardRB` is deliberately NOT a fallback** (a per-Clifford number would understate every gate); the gate metric is offered BEFORE the Clifford one; patches read `2Q gate (IRB)` / `2Q Clifford (SRB)` where both read `2Q RB`; α gets its own "RB fit (decay α)" section as bare numbers; and `2Q Bell` → `2Q gate fidelity` (that tile reads `cz_fidelity`, whose source is Bell / IRB / CR channel — already wrong on a CR chip). **The stored metric choice is versioned** (`quam_topo_hero_metric_v2`, the docs/85 `_cols_v2` precedent): nobody picked the Clifford number on purpose, they got it as a default under a label that did not say what it was. **Clifford→gate IS derived now (user-approved).** `core/rb_gate_fidelity.py` follows `StandardRB_load_id` to that run's `data.json` and READS the `average_gate_fidelity` the node already computed — never recomputes it. Wired into both topology entry points via `routes._topology_with_derived_rb` (deep-copies first: `get_topology`'s result is CACHED) with `_rb_run_folder` doing the bare-run-id lookup WITHOUT `/dataset/by-run`'s rescan-and-retry, because this runs while rendering. Shown BESIDE the Clifford value (`→ 99.44% per gate ÷5.37`) plus a `2Q gate (SRB÷)` hero patch, never instead of it. Degrades to a blank on a missing/older/unreadable run — measured on the demo chip: cz_unipolar 0.967162→0.993886, cz_gaussian_bipolar 0.970461→0.994499, and cz_flattop BLANK because run #2655 is not on disk. The SRB-derived and IRB numbers are independent estimates and agree to 0.4-0.8%. (Correction worth recording: I first reported that divisor as absent everywhere — I had checked one unrelated run and generalised. It is in `#2271/data.json`, and it is 5.371, not the 1.5 one might guess.) Pinned by `tests/test_rb_levels.py` (37 tests, **5/5 + 7/7 + 3/3 mutations** — the last three cover `routes._rb_run_folder`, whose `getattr(run, "folder_path")` was None for every run because `get_run` returns a DICT; the unit tests hand `derive_for_edges` their own resolver, so only a live check caught it) + `chip_status_hero_selfcheck.cjs` (**3/3 mutations**).

**Perf diagnosis + the Param History fix (docs/139, 2026-08-25).** Two measured causes, both fixed. ① Live State Edit's constant 4–5s click: server is 0.5–1s; **the htmx SWAP of the ~MB 452-col grid fragment is 2.4s** + 0.65s settle, and `/bulk` is not in PaneState's KEEP tier at all (`KEEP=['/explorer']`); `/explorer` keep-alive WORKS (`paneRestored` verified; an "empty stash" reading was a stale-alias artifact — `_historyReset` reassigns `stash={}` so the exported `_stash` points at a dead object) but docs/110's "never cancel the request" doctrine means a restore still pays fetch+fresh-insert and discards it. NOT a regression: the always-cold render grew with the grid (docs/94 cap, docs/126 + docs/136 columns). **Fix 1 shipped (2026-08-26)**: `/bulk` joined KEEP and an `htmx:beforeRequest` interceptor cancels the GET when a FRESH parked copy exists (same seq+chip gate as `_tryRestore`), restoring synchronously and pushing the URL itself — measured 4–5 s → **1.0 s** on a RETURN (a cold first visit is 6.9 s and unchanged by design — nothing is parked yet) with zero requests sent; the cancel also carries `stopImmediatePropagation` because three downstream listeners counted the cancelled event as a live xhr and never cleared — NavProgress's bar ticked forever (customer-reported at 154 s) while polling `/api/progress` every 350 ms; `_verifyRestore` stays as the server-truth safety net. Back needed two live-found follow-ons: a `data-pane-route` stamp + mismatch-refetch in `_historyReset` (htmx's `currentPathForHistory` doesn't move on a foreign pushState, so it POISONED its own history cache — content saved under the wrong URL; the repair purges `htmx-history-cache`), and a 1 s dedupe token for the popstate/historyRestore double-fire. Honest residual: Back off the bulk grid freezes ~2.7 s in htmx's own history serialization (pre-existing), and Back after a skip converges ~1–2 s late. Pinned by `pane_state_selfcheck.cjs` (28 asserts, 7/7 mutations). ② Param History 18–22s cold / 0.13s warm: cProfile — `scan_workspace_alignment` is 97%, reading **every run's state.json+wiring.json (2,653 runs × 2 files = 15.1s of read_json)** to fingerprint them, with the cache in-memory only, so every SM restart re-paid it. **Fix 2 shipped (2026-08-25)**: the fingerprint cache persists to `instance/history/_fingerprints.json` (same `(state mtime, wiring mtime)` key — run archives are immutable, so entries never invalidate), loaded lazily with memory-beats-disk `setdefault`, flushed once per alignment scan behind a dirty counter; a corrupt sidecar or bad entry degrades to recompute, never a crash. Measured: restart-case first hit **18–22s → 0.5s** (cold compute 10.6s, 4.6MB sidecar). Follow-on: `_workspace_token` gained an optional `own_root` exclusion (the flush bumped `instance/history`'s mtime, and with the instance dir nested in a workspace root the token sweep read SM's own bookkeeping as "workspace changed", invalidating the scan's own cache) — kept a STATICMETHOD because `routes._dataset_candidate_folders` calls it unbound. Pinned by `tests/test_fingerprint_sidecar.py` (10 tests, 7/7 mutations). ① remains diagnosed-only.

**The night session: apply cost, the Config Manual, search speed, the undo trail (docs/141, 2026-08-27/28).** Autonomous overnight campaign under three rules (accuracy first, tokens frugal, heavy adversarial review after each commit); every number was measured in real headless Chrome / on a copy of the 20Q chip, and the wrong ones were corrected in the doc. ① **Apply-to-live** re-serialised the same content six times — `safe_io.read_json_raw` / `read_state_wiring_raw` / `write_state_wiring_bytes` make a history snapshot and the live write BYTE COPIES of what was hashed (same torn-pair refusal, same rollback); `read_json`/`read_state_wiring` delegate to the raw variants. Production-mode steady-state apply 0.55 → ~0.35 s (the earlier "0.8 s" was TESTING mode with `defer_index` off — a correction). The self-heal readers now JOIN in-flight deferred index threads (`_join_deferred_index`) instead of starting a full rebuild ~100 ms after an apply. Both snapshots kept on purpose. ② **Config Manual** — sidebar item directly below Settings/Calculator (user-directed), a movable body-level window (calculator pattern) with the house search box; `core/key_manual.py` assembles one row per (class, field) from TWO sources, labelled and never blended: the env classes' own Google-style docstrings (`probe_state_schema` parses Args:/Attributes: up the MRO; the schema cache is gated on `SCHEMA_FORMAT` on BOTH the probe and the `cached_only` request path — a review caught that a warm cache would otherwise hide every docstring forever) and the official QM docs for port/config keys (`core/key_manual_docs.py`: `band`, `upconverters`, `full_scale_power_dbm`, `lo_mode`, `sampling_rate`, `upsampling_mode`, `output_mode`, `offset`, `delay`, `filter`, `crosstalk`, `gain_db`, `intermediate_frequency`, `time_of_flight`, `smearing`, `sticky`, `core` — each naming its page#anchor and a verbatim line; a test asserts every anchor is a real heading; entries the pages never state were REMOVED). A key nobody describes says "no description". `node_keys()` answers "what can THIS place carry" (set / unset = keys you could add / undeclared lab extras); F1 on any value, a `?` on LIVE state tree rows (crud renders only), Live-Edit headers and inspector rows opens it. Rebuilt the next day (docs/141 §4h, user-directed): the manual lists EVERY class the selected env offers — `probe_state_schema.py --catalog` enumerates the `QuamComponent` subclass closure after importing quam's components and quam_builder's superconducting architecture by name (never a blind package walk; private ancestors are descended, not listed), cached per env in `state_schema_catalog.json`, warmed quietly on the chip-load thread and by a cold `/api/manual` (`catalog_state: loading` → the open window re-asks) — 111 classes / ~940 keys on the cqt env in 2.4 s. Category › class › key with a used-class open and its keys' places clickable; descriptions in the normal foreground (muted = annotations only); SM-blue rounded resizable window (size remembered). Pinned by `TestCatalog`, the cqt probe pin, `test_config_manual.py`, `config_manual_selfcheck.cjs`. ③ **Search**: the Live-Edit slowness was neither the safety gates nor the matching — the search hid excluded columns with one attribute-equals rule per column and Chrome indexes rules by attribute NAME, so ~8,000 tds tested ~300 rules (250–370 ms style recalc per keystroke). Every th/td now carries `ck-<index>` and the search hides by CLASS selector: 'amplitude' keystroke 925 → 186 ms. The Json tree stays a TREE on a broad query: past 150 matches only the first 150 are materialised — taken SHALLOWEST-FIRST since §4am (2026-08-31, customer report: flat order spent all 150 on ONE slot's leaves once an ancestor key like `analog` matched, because every descendant matches by path; measured 1/5 slots → 5/5, 20/39 ports → 39/39 on the same budget, and the notice is sticky now) — + highlighted under a notice with the true count and a "show all" button (the night's flat result list read as "the tree vanished" and was revised on user feedback): 429 ms block → none. The daytime follow-up (docs/141 §4d) profiled the residual to the phase — JS 34 ms, the rest a WHOLE-grid restyle caused by replacing the hide-stylesheet's text — and fixed it with static rules + `sh-N` class deltas on the table, viewport-only hydration on search, and "a hidden column is cold" (a /bulk opened with a remembered search had virtualization silently OFF): typing `amplitude` letter by letter 1052 → 279 ms blocked; DOM-shrinking not needed. **The mount itself (docs/141 §4i):** `_virtInit` went 1.1–1.8 s → ~0.1–0.2 s (fragments instead of innerHTML, width freeze by class, and NO geometry read — coldness estimated from the inputs' `size` against `screen.availWidth`; `window.innerWidth` forces style+layout in Blink), mount total 1.3–2.2 s → 0.32–0.47 s; the ~2 s before the mount turned out to be the 8.7 MB grid DOCUMENT — server render 1.1 s + parse + first style/layout — not script evaluation, which is ≈0.3 s warm for all 2.2 MB (docs/141 §4l). Late-day perf numbers are noisy: A/B against HEAD in the same session. ④ **Undo (user: very important)**: a Ctrl+Z from the inspector/tree cost the 2.4 s grid re-GET because a path with NO cell counted as uncovered — `revertPaths` now reports only cells it found but could not repaint honestly (`uncovered`), and `undo-trail.js` answers every Ctrl+Z / Ctrl+Shift+Z with a compact bottom-right panel (path, from → to, tier, last 8) whose one button **go to field** is the only thing that navigates (flash if visible, else the owning surface via UndoNav) — true only since the daytime real-Chrome check (docs/141 §4c) retired `cellsReverted`'s auto-navigation, made the tree's `_treeData` follow edit/undo/redo (`_treeModelSet`), and moved an inline field's `data-committed` with the reverted value (a click-away used to re-commit it and wipe the redo stack). **Bursts (docs/141 §4e):** auto-repeat is ignored (one press = one step); presses that arrive while a request is in flight coalesce into `/undo?n=k` / `/redo?n=k` (k actions popped in one lock, every path answered; a `jrn:` step ends a burst); `/redo` answers chronologically so the last client write is the newest value; a Ctrl+Z whose focus is in an inspector/Pulses inline field undoes the typing if dirty, else reaches the server tier (it used to do nothing — the press a person actually makes after Enter); the Pulses committed waveform follows undo/redo from a 20-state RAM cache (`pulses.js` `refreshCommittedPlot`), one synth call per burst on a miss, never the stale plot meanwhile. A same-day adversarial review (docs/141 §4e-review) found 2 major + 6 minor, all fixed: an undo the cache key cannot see (list/runtime row, re-link, a POINTER-TARGET edit — inputs carry `data-target-path`) re-renders the inspector instead of trusting the cache; a burst runs under one `store._lock` and answers `requested/consumed/stopped/level` so a journal-stopped burst re-queues its remaining presses (never dropped silently); the auto-repeat guard sits after the "not ours" bail-out; `_revertTreeNode` writes the model for a never-materialised leaf; the reverted baseline is the lossless `old_value_disp`. Pinned by `pulses_undo_selfcheck.cjs`, `ctrlz_selfcheck.cjs`, `undo_pages_selfcheck.cjs`, `test_undo_journal.py`. **The pulse inspector is a VIEW of up to four pulses (docs/141 §4k, user-directed):** `/pulse/detail?path=<main>&paths=…` renders one plot + one editable parameter section per pulse (`_pulse_section_ctx` per pulse, `_pulse_params_section.html` per section), a pair macro's companions come along by default, Compare opens the same route; one colour per pulse in plot and section; previews/caches/undo refresh are per section; forms carry `view_main`/`view_paths` so a commit keeps the view. **Pulses rows patch in place (docs/141 §4j):** a value change (undo/redo/`/pulse/edit`; the server expands pointer-linked operations) emits `pulses-rows-changed {paths}` and app.js re-renders only those rows via `GET /pulse/row?path=` (`_pulse_row.html`, the table's own partial); `pulses-changed` stays the structural whole-table re-fetch. Two event NAMES on purpose — an htmx `[filter]` needs eval, which the CSP forbids, so a filter is silently ignored (measured). **The Diagnostics findings list follows the state (docs/141 §4f):** `#diag-findings` re-fetches itself via `hx-select` on `diagnostics-changed`/`stateRestored` (it was a one-shot render that stayed red until F5); pinned by `tests/test_diagnostics_refresh.py`. **Page bundles (docs/141 §4l):** `base.html` ships nine core scripts; the other fifteen are page BUNDLES emitted only for the page that uses them (grid / pulses / wiring / chipstatus / components / generate / datasets / scheduler / autofit / compare), one `#bundle-manifest` per page, and `window.Bundles` holds an htmx request (`htmx:confirm` → `issueRequest`) or a Back/Forward restore (htmx's `onpopstate` chained, the ORIGINAL event replayed) until the page's bundles are here — a lost script still issues the request; Settings' grid controls go through `Bundles.call`. Script bytes 2,236 → 976 KB on the light pages. New page-specific JS is born as a bundle; carving the existing app.js is a maintainability decision (the tree viewer is shared UI), recorded with numbers in §4l. Pinned by `tests/test_bundles.py` + `bundles_selfcheck.cjs`. **The review of the day's nine commits (docs/141 §4l-review, five reviewers, 1 critical + 12 major confirmed, all fixed):** the Pulses row patch went through `htmx.ajax` on the shared body source, where htmx's default `hx-sync` `last` queue DROPPED the middle rows of a burst — rows are fetched with `fetch()`, re-targeted at landing, generation-guarded, and a row that left the page filter answers 204; a path that IS a pulse or that no row owns is structural (`_pulse_rows_touched`), as is a pointer move on either side of an undo; `pulseRootOf` names a pair macro's SLOT; `paths=` is a repeated param; a stale `view_main` no longer turns a landed write into a dropped 404; the Config Manual's row budget emptied every class past ~400 keys (lazy past the budget now), a failed probe is remembered (`catalog_state` error/partial, backoff), a partial catalogue is never cached, classes group by PATH; the grid's freeze pin was vacuous (fixture had no `ck-N`) and an undo of a pair path un-virtualized the whole grid (`byPath` map); the loader uses plain `issueRequest()` (the skip flag skipped `hx-confirm`) and a per-target sequence for `hx-sync=replace`. Tooling: `cdp_measure.js`/`cdp_act.js`/`cdp_shot.js` drive Chrome headless over the DevTools protocol (Node's built-in WebSocket) for real long-task/trace splits and screenshots — the browser extension cannot reach this machine's localhost. Pinned across `test_safe_io_raw`, `test_deferred_index_join`, `test_key_manual*`, `test_config_manual` + `config_manual_selfcheck.cjs`, `test_undo_trail` + `undo_trail_selfcheck.cjs` + `tree_search_list_selfcheck.cjs`, `bulk_search_selfcheck.cjs`, `ctrlz_selfcheck.cjs`; every new pin mutation-checked. **The /bulk document itself (docs/141 §4m, 2026-08-29, user-directed):** the 20Q chip's grid was 9.0 MB of which 51% was whitespace (one blank indented line per Jinja block in every cell, eight attribute lines per input) plus a four-span before→after chip and an empty band message rendered in every one of ~8,000 cells. The two cell blocks of `_bulkedit.html` are now single-line with Jinja whitespace control (the ONE space between two inline siblings — quote marks, pointer glyph, ✎ button, ↗ link — is kept and pinned), and `.bulk-ba` / `.bulk-band-msg` are created by the grids on first use (`_ensureBA`, `_ensureBandMsg`). A token-level golden over all 7,810 cells found 0 differences; 8.98 → 3.90 MB, 42k → 21.5k elements, parse+mount long task 738 → 391 ms, ~0.2–0.3 s off `load` in real Chrome. `trim_blocks` stays OFF globally; `data-resolved` / `title` / `data-col-key` dedup deliberately not done (selector semantics, 76 consumers). Pinned by `tests/test_bulk_markup.py` + `bulk_markup_selfcheck.cjs`. **Server-side column virtualization (docs/141 §4n, 2026-08-29, user-directed "option 3 now").** The qubit grid's columns past the client's look-ahead window render COLD on the server — an empty `<td>` keeping its identity (`ck-N`, flags, `data-col-key`) plus one `#bulk-cold-map` value map — and are filled by `GET /bulk/cells?cols=`, which renders the SAME Jinja macro the page used (`_bulk_cell_macros.html`: `qubit_cell` / `pair_cell` are now the ONE renderer of a cell's contents). `core/bulk_virt.py` mirrors the client's `_virtInit` estimate conservatively (px/char = the client's smallest assumption, absent `vw` hint = a 1,920-px screen, the same 600/800 gates — a small chip renders byte-identically); the grid is memoized per context on (`mutation_seq`, change-log length, `dynhide`) via `_qubit_bulk_grid`/`_bulk_grid_cached`; `?chip=` guards a chip swap with 409. The client adopts server-cold tds into the same `_virt` as the client-side detach (marked `remote`), freezes widths from the header's `data-maxlen`, fetches one request per scroll pass with in-flight dedup, keeps a failed batch cold with one honest line and retries; the apply-echo sync and the undo repaint never fetch (a cold column arrives from the working copy when fetched); the edit carry and a sort wait for the fetch. Real-chip golden: cold render + hydration == hot render, 7,810 cells, 0 diffs. The PAIR grid still rendered whole at this point; §4ad does it, in the order this follow-up named — extract, then reuse. The scroll pass hydrates a WINDOW (a jump to the far right no longer fetches every skipped column). Real Chrome on the 20Q chip: document 3,812 → 2,848 KB, first byte 445 → 281 ms, `load` 1,814 → 1,253 ms (cumulative from e898cdb: 8,767 KB / 2,013 ms → 2,848 KB / 1,253 ms). Pinned by `tests/test_bulk_virt_server.py` + `bulk_virt_server_selfcheck.cjs`; mutation-checked 9/9. **Chip Status layout (docs/141 §4o, 2026-08-29, user-directed):** section order Overview · Health · Topology · Trends · Fidelity · Coherence · Frequencies · Calibration (template, sub-nav, sidebar sub-links, `TAB_SPEC` all agree; `?view=gate` still accepted and mapped onto Fidelity); the Health-row "Tiles" slider is gone — every metric / 2Q-gate panel carries its own S · M · L right of its title (`ChipStatus.density`, per-panel `--topo-density-scale` on the `.topo-section`, persisted in `quam_chip_density_panels`); "Gate (2Q)" is absorbed by Fidelity (2Q RB block first, then 1Q, then readout); the IQ-blob metric is **Readout Fidelity (GE)** / **(GEF)** everywhere (glossary abbr "Read. Fid. (GE)"), with `assignment_fidelity_gef` a real node metric (`query._assignment_fidelity_n`, mean diagonal over all n states), a threshold, a Trends label and a tracked history property (index v4 upgrade). A section wrapper hidden by an unclosed banner comment is now caught by a comment-aware parse of the rendered page. Pinned by `tests/test_chip_status_layout.py` + `chip_density_selfcheck.cjs`. **A new run folder reaches the screen in <1 s (docs/141 §4p, 2026-08-29, user-directed):** push, not faster polling — `core/run_watch.py` (a stat-based daemon thread: root mtime + newest date dir + newest run dir signature every 0.5 s, a tick on change; `watchdog` is not in the customer envs) + `GET /datasets/wait?since=<tick>` (long-poll ≤25 s, roots refreshed from the active dataset folders per call, `since=-1` = handshake answered at once and never as a change) + `web/static/live-wake.js` (core script: ONE wait open, wakes `sm:runs-changed` → the new-run popup poll and `DatasetVirtual.pollNow()` → the Datasets delta poll, hidden-tab pause, backoff). The existing 60 s / 5 s polls stay as the safety net; both consumers have an in-flight guard; the popup poll now baselines 1.5 s after load (its first run never pops). Real Chrome: popup 221–544 ms after the folder appeared. Pinned by `tests/test_run_watch.py` + `live_wake_selfcheck.cjs`. **One scrollbar (docs/141 §4q, 2026-08-29, user-directed):** `#table-pane` is the ONE vertical scroller on Live State Edit and Chip Status — `.bulk-table-wrap` is a `max-content` frame (no overflow, no height cap), the top scrollbar proxy is gone, the sticky header / row heads / Apply column anchor to the pane with its padding subtracted (`--table-pane-pad-v/h`), cold-column hydration listens to the pane (`_scrollerOf`), and the toolbar rows / chip bar / pair divider follow a sideways scroll by `translateX(scrollLeft)` (`_pinBarsToScroll` — `position:sticky` cannot hold a row as wide as its containing block, real Chrome); `.topo-hero-scroll` grows to its map (sideways scroll only when zoomed wider). Pinned by `tests/test_single_scroll.py` + `bulk_virt_server_selfcheck.cjs`.  **Popovers + the Pairs picker (docs/141 §4s):** the §4q `will-change: transform` on the toolbar rows made each a stacking context and the chip bar painted over the Properties/Qubits menus — gone; the rows are z-ordered explicitly (toolbar 8 > chip bar 6 > sticky header 4/2). `⚯ Pairs` sits right of `⚏ Qubits` and picks the pair grid's rows (per-chip `quam_bulk_qhidden:pairs:<chip>`, "N of M pairs — Show all" pill, folded into the qubit-follow rule, a dirty pair never hides). Pinned by `tests/test_bulk_pairs_picker.py` + `bulk_pairs_picker_selfcheck.cjs`. **Three tool windows, one frame, one drag core (docs/141 §4u):** `web/static/float-panel.js` (core script, before calc.js/manual.js) is the ONE drag implementation for the Calculator, Settings and the Config Manual (4 px click threshold, viewport clamp, header buttons excluded, `unfloat`); Settings has a header and floats (`settings-floating`); the two tools are two windows, never a singleton — neither toggle closes the other and both outside-click closers ignore the other tool's button/window (the second path that made the Calculator vanish); one frame rule for the three panels placed AFTER their own rules (source order decides at equal specificity). Pinned by `tests/test_float_panel.py` + `float_panel_selfcheck.cjs` + the rewritten `sidebar_tools_selfcheck.cjs` §4/§7. **One frame on the shared modal shell (docs/141 §4v):** `.tfx-host` (strips the `.ch-card` shell under the repair / schema-review dialogs) is `.ch-card.tfx-host` — as a single class it lost to `.ch-card` by source order and both frames painted. Pinned by `tests/test_modal_frames.py`. **The Json tree's `?` is hover-revealed and sits right of the row's ✎ ⧉ ＋ ✕ (docs/141 §4w)** — `_buildRowActions` re-appends `.tree-help` after the lazily built group; `.key-help-btn.tree-help` is opacity 0 at rest. Pinned by `tree_help_hover_selfcheck.cjs`. **A column drag moves ONE column (docs/141 §4x)** — `enhanceColumnResize` pins the table's width to the sum of its columns once any is manual (under `table-layout: fixed` + `width: 100%` the freed space was redistributed to the other columns). Pinned by `col_resize_selfcheck.cjs`. **Compare Selected → Diff, figures first, ticks kept, 2–5 sources (docs/141 §4y)** — `/compare` and `/diff/runs` answer `HX-Location` into `#table-pane` (`_pane_redirect`; an `HX-Redirect` rebuilt the sidebar and lost the ticks), `_DIFF_TABS` opens on figures for runs, `_DIFF_SLOTS = "abcde"` / `_DIFF_MAX_SOURCES = 5` with the sixth tick refused client-side and <2/>5 refused by name server-side; the Compare hub is retired as a destination. Pinned by `sidebar_compare_selfcheck.cjs` + `test_sidebar_compare.py`. **The pane view (docs/141 §4z)** — `/diff?…&view=panes&base=k`: one pane per source (`_diff_panes.html`, `diff-panes.js` in the 'compare' bundle), only differing leaves (`json_diff.diff_rows_n`), cells classed against the BASELINE from the server-stamped PAIRWISE equality matrix (`_diff_row_eq`, `data-eq` — never a JS equality; group IDS could not express the rule, see §4ac), Δ only where a cell differs; 3+ sources are always panes, two get a Panes toggle; the baseline rides `htmx:configRequest` (htmx captures paths at init — rewriting `hx-get` does nothing). Pinned by `diff_panes_selfcheck.cjs` + `test_diff_panes.py` + `test_diff_three_way.py`. **Sidebar root row (docs/141 §4aa)** — name first, parent path ellipsized, a reserved control column whose rule must outrank `#sidebar details > summary` (specificity, not source order). Pinned by `test_sidebar_root_row.py`. **The pane view's Keys column is a tree (docs/141 §4ab)** — `_diff_tree_rows` emits dir/leaf rows depth-first (only containers on the way to a differing key), collapse is a client ancestor walk over `data-parent`, Depth buttons, keys full-size/full-contrast. **Collections' tag-filter row renders only when a tag exists (docs/141 §4t)** — with none it was a lone "All" chip under the Experiments filter; pinned in `test_web.py::test_collections_without_any_tag_has_no_tag_row`. **The review round over §4m–§4ab (docs/141 §4ac, 2026-08-30):** seven reviewers, then a VERIFIER per dimension whose brief was to refute, then a completeness critic and a **fix-risk critic** — 5 CRITICAL / ~28 MAJOR / ~45 MINOR raised, 2 refuted, and **five of the proposed fixes were wrong** (two that cancelled each other, one that turned a 22 s freeze into ~73 req/s, one that silently switched §4o's feature off, one that was a measured no-op). The CRITICALs: the pane view's Show-more button destroyed the comparison (`join('&amp;')` double-escaped, so every slot after `a=` was dropped); a capped N-way diff INVENTED its "agree" count (`same = len(paths) - len(rows)` counted every unexamined path as agreeing — 5,957 where the truth was 890, with 5,354 differing leaves hidden and no Show-more left); a REMEMBERED search left the grid empty forever (the mount's first `applySearch` runs before `_virtInit`, so no server-cold value was in the haystack — "0 of 20", every row hidden); four SM tabs froze the UI for 22 s (`live-wake.js` is a core script and `qsm serve` runs waitress at `threads=4` — now 16 threads + a per-app semaphore + a `saturated` client back-off, all three or none); and returning to Live State Edit painted the whole toolbar 3,000 px off screen (PaneState parks the bars but not the pane's `scrollLeft`, which §4q made their transform a function of). **Equality classes cannot model `compare_equal`**: it carries a relative tolerance, so `_eq` is not transitive and first-match grouping made the pane paint two cells as different that the app's one rule calls equal — and the answer changed with slot order; rows carry the pairwise matrix now, and `diff_rows_n` compares every side against every representative rather than only the first. `test_single_scroll.py` had been **RED since §4s** (it indexed a selector that commit deleted, and asserted the `will-change` it removed *because it was the bug*) — four reviewers found it independently; `version_diff_selfcheck.cjs`'s ~50% flake was **introduced by §4p**, not inherited (the popup baseline timer), and is 10/10 green now. Also fixed: `/bulk/cells`' chip gate compared the folder BASENAME, so a chip and its backup hydrated each other; the sidebar's "Show all N" wiped every tick while the button still claimed them, and a tick made while the tree was filtered destroyed the ticks outside it (the mirror is the selection model now, merged, with off-screen entries submitted as hidden inputs and named on the button); the third tool window still closed the other two; a floated panel was never re-clamped; `fitTableToColumns` pinned a /pulses table to the width of the window it first rendered in; a NaN passed the confusion-matrix validator; a 2×2 under `gef_confusion_matrix` was scored against three-state thresholds; `?view=gate` reached nothing. **Corrections to §4m/§4n's own numbers**: the pair grid is 3,330 cells / ~1.5 MB (not 1,530 / 0.6 MB) and is now 51% of `/bulk`, the largest block left; "never fewer hot columns than before" is false without the `vw` hint and below the table-size slider's default (the constants were deliberately NOT lowered — it costs 198→164 cold columns — but every width metric the server mirrors is pinned against the client's now). New modules/routes this range added, for the Key Files table: `core/bulk_virt.py` (`GET /bulk/cells`), `core/run_watch.py` (`GET /datasets/wait`), `web/static/float-panel.js` (the ONE drag core for three tool windows), `live-wake.js`, `diff-panes.js`. New storage keys: `quam_chip_density_panels`, `quam_bulk_qhidden:pairs:<chip>`, `quam_bulk_fs`, `quam_bulk_col_widths`, and the sessionStorage `quam_sidebar_compare_sel`. **The pair grid virtualizes too (docs/141 §4ad, 2026-08-31, user-directed).** §4n left the pair grid whole and named the follow-up: *generalize the mechanism into a shared module first.* §4ac measured what that cost — 3,330 cells / ~1.5 MB, **53% of the `/bulk` document**, the largest block left. So the ~470 lines of virtualization inside `bulk-edit.js` became **`web/static/grid-virt.js`** (`GridVirt.create(opts)`, 507 lines): cold/remote sets, value map, width freeze by generated stylesheet, the look-ahead scroll pass, one request per pass with in-flight dedup, `ensureTd` hydration, the failure note. Three rules the split is built on, each mutation-guarded: the core carries **no default for a grid's DOM fact** (`rowAttr` has no fallback — a default of `'data-qubit'` silently half-works for the second consumer, and my own new pin caught that leak mid-extraction); per-instance state is keyed by the instance (the scroll-bound flag is `wrap['_virtScrollBound_' + styleId]`, because §4q put both grids in ONE `#table-pane` and an element-level flag would let whichever mounted first suppress the other forever); and the owner keeps a **live mirror, not a copy** (`bulk-edit.js`'s `_virt` is fed by an `onState` callback at every assignment — the first extraction nulled the state inside the core's own listener and an existing §4n pin caught the stale mirror). `core/bulk_virt.py` needed **no change at all** — a pair column dict already carries the `default_on`/`maxlen` the estimate reads, which is the measured evidence §4n's planner really was generic. Server: `_pair_grid_cached` memoizes the pair grid on the same `_bulk_grid_key`; `/bulk` plans `pair_cold_keys` + `pair_cold_map`; `/bulk/cells` gained `?grid=qubit|pair` where an **unknown grid is a 400, never a fallback** (serving qubit cells into pair rows would put wrong numbers on screen, the one failure this mechanism must never have) and no `grid=` still means qubit. Client: `pair-edit.js` binds its own instance with its own element ids — **the two grids share no element id**, pinned, since a shared style element would mean one grid's width freeze overwriting the other's — and four places learned a cell may not be here yet (search folds the cold map into `rowHay`; `sort` fetches first; `_editableIn` hydrates before treating a cold td as editable, because Tab lands on them constantly; `_revertPaths` patches the **map** for a remote column so an undo costs no round trip). The mount reads **no geometry** (§4i) — the first binding called `onScroll(true)` inline and the new harness caught four layout reads; the pass is deferred. Measured on the 20Q chip: `/bulk` **2,820,554 → 2,304,399 B (−18.3%)**, pair block 867,787 B / 37.7%, cold map 113,440 B, 91 of 111 pair columns cold; cumulative §4m→§4n→§4ad **8.88 → 2.30 MB** as htmx documents (8.98 → 2.40 as full pages); script total **+14.3 KiB**. (The first published figures were character counts labelled bytes, a cold map re-serialised with different JSON separators, a full page compared against a fragment, and a size delta mixing git's LF with disk's CRLF — all corrected in §4ae after independent re-measurement.) Three cell-token goldens on the real chip: pair cold+hydrated vs whole **3,330 cells / 0 diffs**, qubit cells **4,480 / 0 diffs**, and every one of the 2,730 pair cells that differs is **exactly an empty cold `<td>`** — zero wrong values. Real Chrome: a search for a value living only in a cold pair column finds its row, a hydrated pair cell arms/disarms Apply like a hot one, a full-width sweep ends 570 cold / 2,760 hot over 11 calls with 0 failures. **A 400 was retried forever**: chasing a `400 no known column named` seen ONCE and **not reproduced** in three later runs (recorded as an open observation, not a diagnosis) established something that does not depend on reproducing it — a failed batch stayed cold so every later scroll pass re-requested it, which is right for a network error and an endless loop for a 400, because a column the server does not know it will not know a second later; a 400 now retires those columns (their values stay in the map, so search still finds them) and says once what a reload would fix. Pinned by `tests/pair_virt_server_selfcheck.cjs` (29 executed asserts) + `TestGridVirtBinding`/`TestPairGridVirt` in `test_bulk_virt_server.py`; **mutation-checked — and the first published figure, "18 of 19", was NOT EVIDENCE**: the sweep ran one trial per mutation against a pytest set containing the test this commit made ~25% flaky (§4ae), so every `1 failed` line is a coin toss reported as a verdict. Re-run with that test deselected: **9 of 11 caught, two caught by nothing** — serving the pair branch through the QUBIT macro (the very failure the commit message called the one this mechanism must never have; 1,660 of 2,910 real cells differ under it) and the 400-retire branch's `onState`. A separate mutation IS a genuine no-op, not a gap: a real pair row is `<tr data-qubit="{{ row.id }}" data-pair="{{ row.id }}">` (`_bulkedit.html:266`), so passing the wrong `rowAttr` selects the same string on every row and changes nothing observable — a pin cannot fail on a mutation that does not mutate. The rule it was meant to test is a DESIGN rule, guarded by a design pin: reintroducing the core's default fails `TestGridVirtBinding::test_the_core_knows_nothing_about_qubits_or_pairs`, verified by mutation. Fixture lesson: the planner's own floors make a small fixture vacuous (40×12 = 480 cells is under `MIN_CELLS` 600; 40×30 gives 780 cold cells, under `MIN_COLD` 800) — 30 pairs × 14 macros is the one that proves anything. **And a harness lesson of mine**: the script that wired `grid-virt.js` into the harnesses truncated a declaration in four of them, and the loop I checked with used `tail -1` — a hard Node crash's last line is `Node.js v24.18.0`, so two read as green. The canonical loop in this file uses the **exit code**; that is why. 95/95 green over two consecutive runs. **The review round over §4ad (docs/141 §4ae, 2026-08-31, user-directed).** Nine agents over ONE 20-file commit — six reviewers (the shared core and whether extracting it changed the qubit grid / the pair client's call sites / the server half / **the pins themselves**, 46 mutations / every claimed number / the seams), then a fix-risk critic and a completeness critic. **6 CRITICAL / 19 MAJOR / 13 MINOR; five proposed fixes were wrong; three of the four behaviour CRITICALs were found INDEPENDENTLY by two reviewers each, on the real 20Q chip.** Every behaviour CRITICAL is the same shape — *the pair grid lacking a guard the qubit grid has had since §4n*, because the mechanism moved into a shared core and the CALL SITES did not: ① `sort()` on a cold column re-entered unconditionally when the hydration promise settled, and `fetchCells` always resolves while a network error or 409 deliberately keeps the column cold — **401 requests in 4 s from one header click in real Chrome, and 148 s of CPU with no output in jsdom (a frozen tab, not a busy one)**; ② `applySearch` never scheduled a pass, so a search or a Properties/Show-all reveal left **110 of 111 pair columns visible-but-blank** until the user happened to scroll; ③ an undo into a client-detached column left the pre-undo value AND its `data-orig`, so the cell read CLEAN and `missing` (never `uncovered`) meant nothing resynced — severity corrected by the critic from "92 of 111" to **1 column of 111** at the default table size; ④ 🕘 Column History was dead on **92 of 111 pair and 198 of 224 qubit columns** (a §4n gap §4ad widened to 82% of a grid). **Two CRITICALs were about verification itself.** The extraction made a shipped pin ~25% flaky, and the cause is a bug that was never there: the parent bound `addEventListener('scroll', _virtOnScroll)`, so the DOM passed the **Event object** as the `immediate` argument and every pass ran SYNCHRONOUSLY — the rAF throttle beside it was dead code for all of §4n. Wrapping the listener revived it, settle moved 79 → 106 ms past the harness's `tick(90)`, and four reviewers measured the rate independently against a 0/67 parent. **And so §4ad's "18 of 19 mutations caught" was not evidence**: one trial per mutation against a pytest set containing that newly-flaky test makes every `1 failed` a coin toss reported as a verdict. Re-run with it deselected, **9 of 11**, and two mutations are caught by NOTHING. **Conflicts adjudicated by measurement**: a partially-unknown batch (a 200 carrying `unknown`, which no client read) does converge — "forever" refuted — but during a scroll sweep the column is blank with no explanation and the note that does fire is erased by the next success; reachability is not theoretical, since **57 of the 111 cold pair columns carry exactly one non-null value** and one `null` edit drops a whole column server-side. §4ad's `rowAttr` no-op claim was re-derived and CONFIRMED. **The completeness critic's seven unrun modalities**: the headline bytes are UNCOMPRESSED (the wire truth is 171,492 → 114,466 B and +5 KB of script, so "+19 KB against −511 KB" is ~9× off, and §4ad gave no timing at all); the failure note is created **23,676 px off screen** (§4q's transform runs only from the scroll listener, which the failure path never reaches); retirement keyed on 400 alone meant **118 req/s offline and 72.7 req/s on a 304**, with a 409 retried forever while its own note said "reload the page", and no `AbortController` against a rule docs/37 §4.1 established; `app.config["contexts"]` is **never popped** so the LRU frees nothing (13 contexts, 65.8 MB vs a documented 40); blocking `grid-virt.js` leaves **6,690 of 7,810 cells blank with no error and no note**; `pxPerChar` reads `documentElement.style.fontSize`, which NOTHING writes (the real root is Pico's 21 px ladder — −14.3% at default, −24.4% at "small", and 206 of 289 cold columns grew on hydration); accessibility has **zero pins anywhere in this project** and 7,200 of 7,810 cells now read as `role=cell name=""`; and `bulk_cells` logs nothing, which is why §4ad's own unreproduced 400 was undiagnosable. Plus a CRITICAL in a neighbour: **PaneState's `_park` reads `scrollTop`/`scrollLeft` AFTER emptying the pane**, so every keep-route restore lands at 0,0 and §4ac's `scrollX` fix is a measured no-op. **Refuted, which is worth as much**: printing, every export path (all model-derived), Ctrl+F (§4ad costs zero find-able strings — the real loss was §4n's and is stated nowhere), copy/paste, a wide monitor, a pair-heavy chip silently disabling virtualization, degenerate chips, and twelve hostile pair ids — and the central safety property holds STRUCTURALLY (pair∩qubit column-key intersection 0, row-id overlap 0, so a mis-routed response can only blank a cell, never show a wrong number). **The five wrong fixes**: the `unknown`-retirement fix as proposed is a measured partial no-op (it bumps `failed`, and the next statement resets it and calls `note('')`); the generation-hash gate has the same false positive its own author rejected for `seq` (creating a leaf ADDS a column, so every later hydration 409s until reload) — shipped as one line, `if (html == null) return;`; loading `grid-virt.js` into five more harnesses is a measured no-op (their fixtures hold 2–9 tds against a `MIN_CELLS` floor of 600); the "tighter" apply-echo fix is WORSE than the broad one (`byPath` is last-writer-wins and **264 real pair paths are claimed by more than one column**); and one severity was two orders of magnitude high. Fixed in the critic's order: harness integrity first (settle conditions instead of a wall clock, **3/10 → 35/35 green**; the vacuous dedup assert's `tick(5)` → `tick(40)`, mutation-verified), then every number, then the sort guard and Column History (one-shot flag set BEFORE the call and stored on the button, not in module scope), then the retirement chain as ONE commit (a `v.dead` set; the instance stays alive while anything is in it so a retired column's value stays searchable; a success clears a RETRYABLE failure and never a retirement; the 200's `unknown` is read — keyed on `unknown`, never on a missing `cells[k]`, because absence is also a legitimately empty answer; a 409 retires like a 400; the note is pinned to `scrollLeft`), then an omitted row staying cold, then the search pass, the undo's local hydration, the broad apply-echo hydration and a cold guard on the pair `_recomputeStats`. **Still open and recorded, not dropped**: the pair fixture is still poorer than the page (static geometry, zero client-detached columns, one cell kind), which is why most of §4ae's own first-draft pins stayed green under mutation — a faithful rebuild was attempted and reverted when it broke two existing asserts for a real reason. **The follow-up round closed seven of those eight (same day, eight agents, one open item each, every one required to return a patch, a pin, and a mutation that fails without it).** The silent failures now speak: blocking `grid-virt.js` left **6,690 of 7,810 cells blank with no error and no note** — the extraction's own cost, since before it that code lived in `bulk-edit.js` where losing it broke the grid visibly — so both grids detect a missing core through one shared `window.GridVirtMissingNote`; `bulk_cells` logs its `unknown` columns through the house idiom (what §4ad's unreproduced 400 needed and did not have); and `.bulk-td-cold` finally has a rule, so LOADING / RETIRED / GENUINELY-ABSENT stop being one appearance. **PaneState's `_park` reorder** (it read the scroll offsets after emptying the pane, so every keep-route restore landed at 0,0 and §4ac's `scrollX` fix was a measured no-op). **`pxPerChar` sees the real root font** — primed once at script evaluation so §4i's no-geometry-at-mount rule still holds, proven by the harness's own `__geomReads` counter. **The context leak**: `app.config["contexts"]` assigned in three places and popped in none, fixed with `TestEvictionNeverLosesEdits`' dirty-context invariant intact. **The project's first accessibility pins.** And the three missing pins, where the C5 work found what the critic predicted and one better: rendering the same 3,000 cold pair cells through both macros differs on 1,660 — **1,653 `missing` + 7 `list`, and ZERO scalars** — so on a scalar-only fixture BOTH proposed pins would have been vacuous; hence `_mixed_chip`, an `extras` band (it sorts last, so it lands cold) carrying one column of every kind `pair_cell` branches on, and both pins ship because a `row_id` wrong in BOTH renders reds only the absolute `openPair` identity assert. C12 gave the pair harness the pytest driver it never had (`pytest tests/` was running none of its 39 asserts); C13 pins that `window.__bulkChipKey` publishes the chip TOKEN — a second publication only `pair-edit.js` reads, so the existing chip-gate test never touched it. **Still open**: the jsdom pair fixture's own faithfulness (its agent hit a session limit mid-sweep); `_mixed_chip` closes the SERVER-side half. **The fixture WAS the finding (docs/141 §4af, 2026-08-31, user-directed).** Rebuilding `pair_virt_server_selfcheck.cjs`'s fixture took the mutation score from **5 of 16 to 10 of 16** and its asserts from 29 to 53 — and the five it newly catches are all on the CLIENT-DETACHED path the harness could not reach at all (the sort re-entry loop, an undo into a local cold column, the apply echo's stale twin, a row the answer omits, the note's scroll pinning). Two cheap facts made it possible where §4ae's attempt failed: `init()` calls a HIDDEN column cold and a server cold map bypasses the MIN_CELLS/MIN_COLD floors, so **one hidden column reaches the local path with no geometry change at all** — and **the hidden set is STORAGE, not markup** (`_applyColumnVisibility` recomputes `bulk-col-hidden` from `quam_bulk_hidden_cols_pair_v2` every pass and erased the class written into the fixture's html). **It immediately found three defects, and NONE of them by a pin failing.** ① §4ae's own note-pinning fix had never run: it called `scroller(t)` where the binding is `scrollerOf`, and the `try/catch` swallowed the ReferenceError — dead from the moment it was written and shipped that way. ② The pair grid's stats guard `_skipStat` was DEFINED AND NEVER CALLED, so a cold column's server-rendered min/max were still being wiped (the defect §4l-review already fixed once on the qubit grid); the sweep found it by reporting an anchor MISSING, not by an assert. ③ This section's own first STATS pin was vacuous — it seeded the stat on the HIDDEN column, and `_recomputeStats` blanks a hidden column and returns BEFORE the cold guard, so it passed with the guard deleted; it uses a server-cold column now and reds on the mutation. An audit for the same syntactic shape (every identifier called inside a `try/catch` that the file does not define) returned one more candidate, `getComputedStyle` — the exact docs/123–125 standing-rule shape — and it is **fine, proved not assumed**: a probe loading the committed blob through `new win.Function(...)`, which compiles in the jsdom WINDOW realm, reports `typeof getComputedStyle === "function"` and `pxPerChar() === 9.1264`, the computed path rather than the ladder fallback. **Still not caught, enumerated rather than rounded away**: C3/C4 need a 400 + a later success + a search chained in one world (expressible now, unwritten); A4 lives in `app.js`, which this harness does not load; the qubit-side stats guard's `dead` half needs the same rebuild on the qubit harness; and the settle-vs-clock mutation went green because §4ae B-8's script-eval priming made the harness fast enough for `tick(90)` at that one site (the other five settle sites still protect). **The lesson: a green suite says nothing about the states a fixture cannot enter, and the cheapest way to find those is to mutate the code the fixture is supposed to be guarding and watch what does not move.** **The night session: apply cost, the Config Manual, search speed, the undo trail (docs/141, 2026-08-27/28).** Autonomous overnight campaign under three rules (accuracy first, tokens frugal, heavy adversarial review after each commit); every number was measured in real headless Chrome / on a copy of the 20Q chip, and the wrong ones were corrected in the doc. ① **Apply-to-live** re-serialised the same content six times — `safe_io.read_json_raw` / `read_state_wiring_raw` / `write_state_wiring_bytes` make a history snapshot and the live write BYTE COPIES of what was hashed (same torn-pair refusal, same rollback); `read_json`/`read_state_wiring` delegate to the raw variants. Production-mode steady-state apply 0.55 → ~0.35 s (the earlier "0.8 s" was TESTING mode with `defer_index` off — a correction). The self-heal readers now JOIN in-flight deferred index threads (`_join_deferred_index`) instead of starting a full rebuild ~100 ms after an apply. Both snapshots kept on purpose. ② **Config Manual** — sidebar item directly below Settings/Calculator (user-directed), a movable body-level window (calculator pattern) with the house search box; `core/key_manual.py` assembles one row per (class, field) from TWO sources, labelled and never blended: the env classes' own Google-style docstrings (`probe_state_schema` parses Args:/Attributes: up the MRO; the schema cache is gated on `SCHEMA_FORMAT` on BOTH the probe and the `cached_only` request path — a review caught that a warm cache would otherwise hide every docstring forever) and the official QM docs for port/config keys (`core/key_manual_docs.py`: `band`, `upconverters`, `full_scale_power_dbm`, `lo_mode`, `sampling_rate`, `upsampling_mode`, `output_mode`, `offset`, `delay`, `filter`, `crosstalk`, `gain_db`, `intermediate_frequency`, `time_of_flight`, `smearing`, `sticky`, `core` — each naming its page#anchor and a verbatim line; a test asserts every anchor is a real heading; entries the pages never state were REMOVED). A key nobody describes says "no description". `node_keys()` answers "what can THIS place carry" (set / unset = keys you could add / undeclared lab extras); F1 on any value, a `?` on LIVE state tree rows (crud renders only), Live-Edit headers and inspector rows opens it. Rebuilt the next day (docs/141 §4h, user-directed): the manual lists EVERY class the selected env offers — `probe_state_schema.py --catalog` enumerates the `QuamComponent` subclass closure after importing quam's components and quam_builder's superconducting architecture by name (never a blind package walk; private ancestors are descended, not listed), cached per env in `state_schema_catalog.json`, warmed quietly on the chip-load thread and by a cold `/api/manual` (`catalog_state: loading` → the open window re-asks) — 111 classes / ~940 keys on the cqt env in 2.4 s. Category › class › key with a used-class open and its keys' places clickable; descriptions in the normal foreground (muted = annotations only); SM-blue rounded resizable window (size remembered). Pinned by `TestCatalog`, the cqt probe pin, `test_config_manual.py`, `config_manual_selfcheck.cjs`. ③ **Search**: the Live-Edit slowness was neither the safety gates nor the matching — the search hid excluded columns with one attribute-equals rule per column and Chrome indexes rules by attribute NAME, so ~8,000 tds tested ~300 rules (250–370 ms style recalc per keystroke). Every th/td now carries `ck-<index>` and the search hides by CLASS selector: 'amplitude' keystroke 925 → 186 ms. The Json tree stays a TREE on a broad query: past 150 matches only the first 150 are materialised — taken SHALLOWEST-FIRST since §4am (2026-08-31, customer report: flat order spent all 150 on ONE slot's leaves once an ancestor key like `analog` matched, because every descendant matches by path; measured 1/5 slots → 5/5, 20/39 ports → 39/39 on the same budget, and the notice is sticky now) — + highlighted under a notice with the true count and a "show all" button (the night's flat result list read as "the tree vanished" and was revised on user feedback): 429 ms block → none. The daytime follow-up (docs/141 §4d) profiled the residual to the phase — JS 34 ms, the rest a WHOLE-grid restyle caused by replacing the hide-stylesheet's text — and fixed it with static rules + `sh-N` class deltas on the table, viewport-only hydration on search, and "a hidden column is cold" (a /bulk opened with a remembered search had virtualization silently OFF): typing `amplitude` letter by letter 1052 → 279 ms blocked; DOM-shrinking not needed. **The mount itself (docs/141 §4i):** `_virtInit` went 1.1–1.8 s → ~0.1–0.2 s (fragments instead of innerHTML, width freeze by class, and NO geometry read — coldness estimated from the inputs' `size` against `screen.availWidth`; `window.innerWidth` forces style+layout in Blink), mount total 1.3–2.2 s → 0.32–0.47 s; the ~2 s before the mount turned out to be the 8.7 MB grid DOCUMENT — server render 1.1 s + parse + first style/layout — not script evaluation, which is ≈0.3 s warm for all 2.2 MB (docs/141 §4l). Late-day perf numbers are noisy: A/B against HEAD in the same session. ④ **Undo (user: very important)**: a Ctrl+Z from the inspector/tree cost the 2.4 s grid re-GET because a path with NO cell counted as uncovered — `revertPaths` now reports only cells it found but could not repaint honestly (`uncovered`), and `undo-trail.js` answers every Ctrl+Z / Ctrl+Shift+Z with a compact bottom-right panel (path, from → to, tier, last 8) whose one button **go to field** is the only thing that navigates (flash if visible, else the owning surface via UndoNav) — true only since the daytime real-Chrome check (docs/141 §4c) retired `cellsReverted`'s auto-navigation, made the tree's `_treeData` follow edit/undo/redo (`_treeModelSet`), and moved an inline field's `data-committed` with the reverted value (a click-away used to re-commit it and wipe the redo stack). **Bursts (docs/141 §4e):** auto-repeat is ignored (one press = one step); presses that arrive while a request is in flight coalesce into `/undo?n=k` / `/redo?n=k` (k actions popped in one lock, every path answered; a `jrn:` step ends a burst); `/redo` answers chronologically so the last client write is the newest value; a Ctrl+Z whose focus is in an inspector/Pulses inline field undoes the typing if dirty, else reaches the server tier (it used to do nothing — the press a person actually makes after Enter); the Pulses committed waveform follows undo/redo from a 20-state RAM cache (`pulses.js` `refreshCommittedPlot`), one synth call per burst on a miss, never the stale plot meanwhile. A same-day adversarial review (docs/141 §4e-review) found 2 major + 6 minor, all fixed: an undo the cache key cannot see (list/runtime row, re-link, a POINTER-TARGET edit — inputs carry `data-target-path`) re-renders the inspector instead of trusting the cache; a burst runs under one `store._lock` and answers `requested/consumed/stopped/level` so a journal-stopped burst re-queues its remaining presses (never dropped silently); the auto-repeat guard sits after the "not ours" bail-out; `_revertTreeNode` writes the model for a never-materialised leaf; the reverted baseline is the lossless `old_value_disp`. Pinned by `pulses_undo_selfcheck.cjs`, `ctrlz_selfcheck.cjs`, `undo_pages_selfcheck.cjs`, `test_undo_journal.py`. **The pulse inspector is a VIEW of up to four pulses (docs/141 §4k, user-directed):** `/pulse/detail?path=<main>&paths=…` renders one plot + one editable parameter section per pulse (`_pulse_section_ctx` per pulse, `_pulse_params_section.html` per section), a pair macro's companions come along by default, Compare opens the same route; one colour per pulse in plot and section; previews/caches/undo refresh are per section; forms carry `view_main`/`view_paths` so a commit keeps the view. **Pulses rows patch in place (docs/141 §4j):** a value change (undo/redo/`/pulse/edit`; the server expands pointer-linked operations) emits `pulses-rows-changed {paths}` and app.js re-renders only those rows via `GET /pulse/row?path=` (`_pulse_row.html`, the table's own partial); `pulses-changed` stays the structural whole-table re-fetch. Two event NAMES on purpose — an htmx `[filter]` needs eval, which the CSP forbids, so a filter is silently ignored (measured). **The Diagnostics findings list follows the state (docs/141 §4f):** `#diag-findings` re-fetches itself via `hx-select` on `diagnostics-changed`/`stateRestored` (it was a one-shot render that stayed red until F5); pinned by `tests/test_diagnostics_refresh.py`. **Page bundles (docs/141 §4l):** `base.html` ships nine core scripts; the other fifteen are page BUNDLES emitted only for the page that uses them (grid / pulses / wiring / chipstatus / components / generate / datasets / scheduler / autofit / compare), one `#bundle-manifest` per page, and `window.Bundles` holds an htmx request (`htmx:confirm` → `issueRequest`) or a Back/Forward restore (htmx's `onpopstate` chained, the ORIGINAL event replayed) until the page's bundles are here — a lost script still issues the request; Settings' grid controls go through `Bundles.call`. Script bytes 2,236 → 976 KB on the light pages. New page-specific JS is born as a bundle; carving the existing app.js is a maintainability decision (the tree viewer is shared UI), recorded with numbers in §4l. Pinned by `tests/test_bundles.py` + `bundles_selfcheck.cjs`. **The review of the day's nine commits (docs/141 §4l-review, five reviewers, 1 critical + 12 major confirmed, all fixed):** the Pulses row patch went through `htmx.ajax` on the shared body source, where htmx's default `hx-sync` `last` queue DROPPED the middle rows of a burst — rows are fetched with `fetch()`, re-targeted at landing, generation-guarded, and a row that left the page filter answers 204; a path that IS a pulse or that no row owns is structural (`_pulse_rows_touched`), as is a pointer move on either side of an undo; `pulseRootOf` names a pair macro's SLOT; `paths=` is a repeated param; a stale `view_main` no longer turns a landed write into a dropped 404; the Config Manual's row budget emptied every class past ~400 keys (lazy past the budget now), a failed probe is remembered (`catalog_state` error/partial, backoff), a partial catalogue is never cached, classes group by PATH; the grid's freeze pin was vacuous (fixture had no `ck-N`) and an undo of a pair path un-virtualized the whole grid (`byPath` map); the loader uses plain `issueRequest()` (the skip flag skipped `hx-confirm`) and a per-target sequence for `hx-sync=replace`. Tooling: `cdp_measure.js`/`cdp_act.js`/`cdp_shot.js` drive Chrome headless over the DevTools protocol (Node's built-in WebSocket) for real long-task/trace splits and screenshots — the browser extension cannot reach this machine's localhost. Pinned across `test_safe_io_raw`, `test_deferred_index_join`, `test_key_manual*`, `test_config_manual` + `config_manual_selfcheck.cjs`, `test_undo_trail` + `undo_trail_selfcheck.cjs` + `tree_search_list_selfcheck.cjs`, `bulk_search_selfcheck.cjs`, `ctrlz_selfcheck.cjs`; every new pin mutation-checked. **The /bulk document itself (docs/141 §4m, 2026-08-29, user-directed):** the 20Q chip's grid was 9.0 MB of which 51% was whitespace (one blank indented line per Jinja block in every cell, eight attribute lines per input) plus a four-span before→after chip and an empty band message rendered in every one of ~8,000 cells. The two cell blocks of `_bulkedit.html` are now single-line with Jinja whitespace control (the ONE space between two inline siblings — quote marks, pointer glyph, ✎ button, ↗ link — is kept and pinned), and `.bulk-ba` / `.bulk-band-msg` are created by the grids on first use (`_ensureBA`, `_ensureBandMsg`). A token-level golden over all 7,810 cells found 0 differences; 8.98 → 3.90 MB, 42k → 21.5k elements, parse+mount long task 738 → 391 ms, ~0.2–0.3 s off `load` in real Chrome. `trim_blocks` stays OFF globally; `data-resolved` / `title` / `data-col-key` dedup deliberately not done (selector semantics, 76 consumers). Pinned by `tests/test_bulk_markup.py` + `bulk_markup_selfcheck.cjs`. **Server-side column virtualization (docs/141 §4n, 2026-08-29, user-directed "option 3 now").** The qubit grid's columns past the client's look-ahead window render COLD on the server — an empty `<td>` keeping its identity (`ck-N`, flags, `data-col-key`) plus one `#bulk-cold-map` value map — and are filled by `GET /bulk/cells?cols=`, which renders the SAME Jinja macro the page used (`_bulk_cell_macros.html`: `qubit_cell` / `pair_cell` are now the ONE renderer of a cell's contents). `core/bulk_virt.py` mirrors the client's `_virtInit` estimate conservatively (px/char = the client's smallest assumption, absent `vw` hint = a 1,920-px screen, the same 600/800 gates — a small chip renders byte-identically); the grid is memoized per context on (`mutation_seq`, change-log length, `dynhide`) via `_qubit_bulk_grid`/`_bulk_grid_cached`; `?chip=` guards a chip swap with 409. The client adopts server-cold tds into the same `_virt` as the client-side detach (marked `remote`), freezes widths from the header's `data-maxlen`, fetches one request per scroll pass with in-flight dedup, keeps a failed batch cold with one honest line and retries; the apply-echo sync and the undo repaint never fetch (a cold column arrives from the working copy when fetched); the edit carry and a sort wait for the fetch. Real-chip golden: cold render + hydration == hot render, 7,810 cells, 0 diffs. The PAIR grid still renders whole (follow-up: generalize the mechanism into a shared module first). The scroll pass hydrates a WINDOW (a jump to the far right no longer fetches every skipped column). Real Chrome on the 20Q chip: document 3,812 → 2,848 KB, first byte 445 → 281 ms, `load` 1,814 → 1,253 ms (cumulative from e898cdb: 8,767 KB / 2,013 ms → 2,848 KB / 1,253 ms). Pinned by `tests/test_bulk_virt_server.py` + `bulk_virt_server_selfcheck.cjs`; mutation-checked 9/9. **Chip Status layout (docs/141 §4o, 2026-08-29, user-directed):** section order Overview · Health · Topology · Trends · Fidelity · Coherence · Frequencies · Calibration (template, sub-nav, sidebar sub-links, `TAB_SPEC` all agree; `?view=gate` still accepted and mapped onto Fidelity); the Health-row "Tiles" slider is gone — every metric / 2Q-gate panel carries its own S · M · L right of its title (`ChipStatus.density`, per-panel `--topo-density-scale` on the `.topo-section`, persisted in `quam_chip_density_panels`); "Gate (2Q)" is absorbed by Fidelity (2Q RB block first, then 1Q, then readout); the IQ-blob metric is **Readout Fidelity (GE)** / **(GEF)** everywhere (glossary abbr "Read. Fid. (GE)"), with `assignment_fidelity_gef` a real node metric (`query._assignment_fidelity_n`, mean diagonal over all n states), a threshold, a Trends label and a tracked history property (index v4 upgrade). A section wrapper hidden by an unclosed banner comment is now caught by a comment-aware parse of the rendered page. Pinned by `tests/test_chip_status_layout.py` + `chip_density_selfcheck.cjs`. **A new run folder reaches the screen in <1 s (docs/141 §4p, 2026-08-29, user-directed):** push, not faster polling — `core/run_watch.py` (a stat-based daemon thread: root mtime + newest date dir + newest run dir signature every 0.5 s, a tick on change; `watchdog` is not in the customer envs) + `GET /datasets/wait?since=<tick>` (long-poll ≤25 s, roots refreshed from the active dataset folders per call, `since=-1` = handshake answered at once and never as a change) + `web/static/live-wake.js` (core script: ONE wait open, wakes `sm:runs-changed` → the new-run popup poll and `DatasetVirtual.pollNow()` → the Datasets delta poll, hidden-tab pause, backoff). The existing 60 s / 5 s polls stay as the safety net; both consumers have an in-flight guard; the popup poll now baselines 1.5 s after load (its first run never pops). Real Chrome: popup 221–544 ms after the folder appeared. Pinned by `tests/test_run_watch.py` + `live_wake_selfcheck.cjs`. **One scrollbar (docs/141 §4q, 2026-08-29, user-directed):** `#table-pane` is the ONE vertical scroller on Live State Edit and Chip Status — `.bulk-table-wrap` is a `max-content` frame (no overflow, no height cap), the top scrollbar proxy is gone, the sticky header / row heads / Apply column anchor to the pane with its padding subtracted (`--table-pane-pad-v/h`), cold-column hydration listens to the pane (`_scrollerOf`), and the toolbar rows / chip bar / pair divider follow a sideways scroll by `translateX(scrollLeft)` (`_pinBarsToScroll` — `position:sticky` cannot hold a row as wide as its containing block, real Chrome); `.topo-hero-scroll` grows to its map (sideways scroll only when zoomed wider). Pinned by `tests/test_single_scroll.py` + `bulk_virt_server_selfcheck.cjs`. **Sidebar "Compare Selected" with exactly two runs opens the Diff workbench (docs/141 §4r):** `POST /compare` redirects two sources to `/diff?a=…&b=…` (two archive runs → `tab=node`, else `state`); three or more still go to the Compare hub. Pinned in `test_compare_hub_routes.py::TestP4Redirects` + `test_web.py`. **Popovers + the Pairs picker (docs/141 §4s):** the §4q `will-change: transform` on the toolbar rows made each a stacking context and the chip bar painted over the Properties/Qubits menus — gone; the rows are z-ordered explicitly (toolbar 8 > chip bar 6 > sticky header 4/2). `⚯ Pairs` sits right of `⚏ Qubits` and picks the pair grid's rows (per-chip `quam_bulk_qhidden:pairs:<chip>`, "N of M pairs — Show all" pill, folded into the qubit-follow rule, a dirty pair never hides). Pinned by `tests/test_bulk_pairs_picker.py` + `bulk_pairs_picker_selfcheck.cjs`. **Three tool windows, one frame, one drag core (docs/141 §4u):** `web/static/float-panel.js` (core script, before calc.js/manual.js) is the ONE drag implementation for the Calculator, Settings and the Config Manual (4 px click threshold, viewport clamp, header buttons excluded, `unfloat`); Settings has a header and floats (`settings-floating`); the two tools are two windows, never a singleton — neither toggle closes the other and both outside-click closers ignore the other tool's button/window (the second path that made the Calculator vanish); one frame rule for the three panels placed AFTER their own rules (source order decides at equal specificity). Pinned by `tests/test_float_panel.py` + `float_panel_selfcheck.cjs` + the rewritten `sidebar_tools_selfcheck.cjs` §4/§7. **One frame on the shared modal shell (docs/141 §4v):** `.tfx-host` (strips the `.ch-card` shell under the repair / schema-review dialogs) is `.ch-card.tfx-host` — as a single class it lost to `.ch-card` by source order and both frames painted. Pinned by `tests/test_modal_frames.py`. **The Json tree's `?` is hover-revealed and sits right of the row's ✎ ⧉ ＋ ✕ (docs/141 §4w)** — `_buildRowActions` re-appends `.tree-help` after the lazily built group; `.key-help-btn.tree-help` is opacity 0 at rest. Pinned by `tree_help_hover_selfcheck.cjs`. **A column drag moves ONE column (docs/141 §4x)** — `enhanceColumnResize` pins the table's width to the sum of its columns once any is manual (under `table-layout: fixed` + `width: 100%` the freed space was redistributed to the other columns). Pinned by `col_resize_selfcheck.cjs`. **Compare Selected → Diff, figures first, ticks kept, 2–5 sources (docs/141 §4y)** — `/compare` and `/diff/runs` answer `HX-Location` into `#table-pane` (`_pane_redirect`; an `HX-Redirect` rebuilt the sidebar and lost the ticks), `_DIFF_TABS` opens on figures for runs, `_DIFF_SLOTS = "abcde"` / `_DIFF_MAX_SOURCES = 5` with the sixth tick refused client-side and <2/>5 refused by name server-side; the Compare hub is retired as a destination. Pinned by `sidebar_compare_selfcheck.cjs` + `test_sidebar_compare.py`. **The pane view (docs/141 §4z)** — `/diff?…&view=panes&base=k`: one pane per source (`_diff_panes.html`, `diff-panes.js` in the 'compare' bundle), only differing leaves (`json_diff.diff_rows_n`), cells classed against the BASELINE from server-stamped equality groups (`_diff_row_groups` — never a JS equality), Δ only where a cell differs; 3+ sources are always panes, two get a Panes toggle; the baseline rides `htmx:configRequest` (htmx captures paths at init — rewriting `hx-get` does nothing). Pinned by `diff_panes_selfcheck.cjs` + `test_diff_panes.py` + `test_diff_three_way.py`. **Sidebar root row (docs/141 §4aa)** — name first, parent path ellipsized, a reserved control column whose rule must outrank `#sidebar details > summary` (specificity, not source order). Pinned by `test_sidebar_root_row.py`. **The pane view's Keys column is a tree (docs/141 §4ab)** — `_diff_tree_rows` emits dir/leaf rows depth-first (only containers on the way to a differing key), collapse is a client ancestor walk over `data-parent`, Depth buttons, keys full-size/full-contrast. **The diff gets a search box (docs/141 §4ai, user-directed)** — one partial (`_diff_search.html`) for the pane AND list views, `window.SearchQuery`'s grammar (docs/96), matching the dot path plus every pane's value raw and as rendered; containers are kept and re-counted, a hit's ancestors auto-expand and the collapse state is restored on clear (unless the user changed it meanwhile), Escape clears, `/` focuses it (it is an `input[type=search]`), and `?q=` rides `htmx:configRequest` as a parameter so a tab switch keeps it — the server echoes it and deliberately never filters by it, since the counts above the table describe the whole diff. Honest about the paged rest (`· N more not loaded`) and about an empty result (a note under the table). Pinned by `diff_panes_selfcheck.cjs` (60) + `TestSearchBox`. **One search hint, and three window fixes (docs/141 §4aj, user-directed):** `core/search_query.search_hint()`/`search_title()` (Jinja globals) are the ONE placeholder for all 16 search boxes — **`Search: space = AND, | = OR`**, a surface's own scopes appended after the grammar, the full sentence in the `title`; a hand-written search placeholder is a test failure (three id-exempt name PICKERS aside). `filterTable`, `filterDetailPanel` and the all-values grid were AND-only, so they now compose through `SearchQuery` — the promise had to become true. Eleven `window.SearchQuery ? SearchQuery.…` sites (guard names window, call reads a bare global — the docs/125 trap) were window-qualified. Also: the Settings window's top corners were its own sticky header painting over the radius (only two of the three tool windows have an `overflow` that clips — the HEADER is rounded now, 8.5px = 10 − 1.5); the Calculator is `resize: both` on a clipped flex column with a scrolling body and a remembered size (`quam_calc_size`); and its section titles were muted because Pico's `details summary:not([role])` (0,1,1) and `details[open] > summary…` (0,3,1) beat `.calc-sec-label` (0,1,0) — fixed by setting Pico's own accordion variables, with sections turned into cards and Pico's duplicate `summary::after` chevron hidden. Pinned by `tests/test_search_hint.py` + `search_hint_selfcheck.cjs`. **Collections' tag-filter row renders only when a tag exists (docs/141 §4t)** — with none it was a lone "All" chip under the Experiments filter; pinned in `test_web.py::test_collections_without_any_tag_has_no_tag_row`.
